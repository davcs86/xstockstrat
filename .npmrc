# Hoist @bufbuild/* to the workspace root so that packages/proto/gen/ts/dist/*.d.ts
# can resolve @bufbuild/protobuf and its subpaths (@bufbuild/protobuf/codegenv2, etc.)
# during TypeScript compilation in Docker (where the proto package has no node_modules).
public-hoist-pattern[]=@bufbuild/*

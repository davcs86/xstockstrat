# Context: shadcn-migration-high-confidence  (archived 2026-08-19)

**Feature**: ./feature.md
**Status**: launched — archived by /sdd-archiver; verbose specs pruned (recoverable via git history).

## Archive Synthesis — 2026-08-19 — /sdd-archiver

**What**: Swapped 27 high-confidence hand-rolled UI reimplementations across trader/insights/config-ui/mobile/auth for 10 shadcn primitives (Tabs, Toggle Group, Alert Dialog, Alert, Checkbox, Textarea, Breadcrumb, Accordion, Progress + adopted Badge/Skeleton). 36 steps, 255/255 e2e + 80/80 unit green. The real story is the two primitive-inherent e2e-collision classes it surfaced and the ordering discipline that contained blast radius — most of which lived only in the execute-session context.

**Why (irrecoverable rationale)**: The design deliberately interleaved each primitive-add with its lowest-risk consumer wire in the same tier (not batching all 8 first) so an integration-fit mismatch surfaces while that primitive's own step is still open (F-09 rework-after-close avoidance) instead of tiers later as a late patch. This ordering + the mandatory two-step red-before-green split per confirmed e2e-risk site (P-06) is *why* every Radix-role break was caught at its own step.

**Rejected alternatives**: Batch all 8 primitives before any call-site migration — mismatch surfaces no signal until tiers later, forcing an F-09 late patch. Single-step component-swap + e2e-spec-update for risk sites — no red-before-green checkpoint; swap-then-rewrite-selector can pass green while silently dropping a behavior the old selector protected. Replace CardNotice.tsx's Card/CardContent with a bare Alert — changes visible box chrome for all 4 consumers, out-of-scope visual redesign.

**Scars & gotchas**: A shared hand-rolled control in a non-FR-cited sibling file: ChartPanel.tsx's timeframe switcher was classified "no e2e-risk" but chart-panel.spec.ts had 3 `getByRole('button',{name:'15m'|'1h'|'1d'})` assertions; the switcher pattern repeats across trader/insights and recon only swept the FR-cited file. Radix single-type Toggle Group uses role="radio"/"radiogroup" + aria-checked, NOT aria-pressed (confirmed against @radix-ui/react-toggle-group source). Tabs.Trigger renders role="tab". shadcn Alert root carries role="alert" unconditionally but AlertDescription alone does not (CardNotice.tsx needed explicit role={variant==='error'?'alert':undefined}). AlertDialogAction auto-closes on click by default; accountShared.tsx's async handleRemove needed event.preventDefault() to keep the dialog open + disabled during the in-flight call. CLI-generated progress.tsx shipped with NO cva()/variant system; buy/paper/sell/muted variant infra was hand-authored, fill driven by Indicator's transform: translateX(-${100-value}%), not inline style={{width}}. Dev-server e2e unusable in sandbox (on-demand-route-compile timeout); ran via CI-equivalent prebuilt path (CI=1 E2E_PREBUILT=1 NEXT_DISABLE_STANDALONE=1).

**Permanent deviations**: design/recon said ChartPanel.tsx timeframe switcher = "no e2e-risk" → shipped had to fix chart-panel.spec.ts (role="button"→role="tab") because a shared hand-rolled switcher pattern also lived in that non-FR-cited file. design said the Breadcrumb case-collision risk was "likely moot" at PlatformHeader.tsx:260-269 → that site passed unmodified, but two *unanticipated* sites broke: page-level Breadcrumbs' default aria-label="breadcrumb" collided case-insensitively with the shell's "Breadcrumb" landmark (fixed via "Namespace path"/"Audit log path"), and BreadcrumbPage's built-in role="link" collided with a real nav Link on /insights/strategies (fixed by scoping backfills.spec.ts, caught only at the Step 35 full-suite gate) — design's risk model named the wrong site (already fails.md:54).

**Cross-feature signal**: The two collision classes here (Radix-role-change breaks getByRole; a primitive's built-in implicit role/label collides with other elements on the same page/sibling files) recur across the whole shadcn family (119, 121-135) sharing the same primitive set + shell (fails.md:54 flags 121/122/123; insights.md 2026-08-10 shadcn-sidebar generalizes "check the actual rendered Radix role").

**Deferred follow-ons**: The 22 medium + 4 low-confidence audit occurrences are siblings 121/122 (custom composites → 123); same shell + primitives, so the collision lessons apply directly.

**Ledger entries written**: insights.md (2), fails.md (1) — see the 2026-08-19 entries.
**Runtime-invariant recommendations (→ /context-constitution)**: UI-* candidates for ui/docs context-constitution (producer contracts of the vendored library): (1) single-type Toggle Group → role="radio"/"radiogroup"+aria-checked; (2) Tabs.Trigger → role="tab"; (3) shadcn Alert root owns role="alert", AlertDescription does not; (4) AlertDialogAction auto-closes unless onClick preventDefault(); (5) CLI-generated progress.tsx has no cva, drives fill via Indicator transform:translateX.
**Pruned artifacts**: product-spec.md, recon.md, design.md, implementation-spec.md — last present at 6a796bc9.

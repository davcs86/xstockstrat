# Context: surface-signal-weight-decay-config  (archived 2026-08-31)

**Feature**: ./feature.md
**Status**: launched — archived by /sdd-archiver; verbose specs pruned (recoverable via git history).

## Archive Synthesis — 2026-08-31 — /sdd-archiver

**What**: Completed two deferred items from features 022 and 134: registering `analysis.scoring.signal_decay_half_life_hours` as a real, server-bounded config key (feature 022 context.md:20 recorded its seed migration as deferred), and surfacing per-source `reliability_weight` through the agent MCP tools (feature 134 shipped the proto field but both the list re-projection and the write builder silently dropped it). The design scope was intentionally expanded at the gate from the product-spec's "guidance-only, no proto changes" to "server-side enforced scalar bounds via a new proto enum value," with operator sign-off. The dead `analysis.signals.source_weights` key and its entire FLOAT_MAP validation machinery were also cleaned up.
**Why (irrecoverable rationale)**: The operator chose enforced bounds over guidance-only because guidance-only ships zero unit-typo protection and the agent's `set_config` path would remain wide-open. The proto change (`VALUE_TYPE_FLOAT_SCALAR = 2`) was accepted as non-breaking (additive enum, `buf breaking` passes) — an explicit override of the product-spec's "no proto changes" clause (C-11 override recorded context.md 2026-08-26 sdd-design session). Removing all FLOAT_MAP machinery was chosen because leaving live-but-zero-coverage code after deleting its sole key ships a maintenance trap (fails.md/074 pattern), per operator Fork-2 sign-off. The `FLOAT_MAP = 1` proto member was retained with `[deprecated = true]` for `buf breaking` wire stability. Key design guards: (a) the round-3 fail-open catch — the string-only read at `configServiceImpl.ts:385` coerces `float_val` to `''` → `Number('')` = 0 → passes `[0, 8760]` unguarded; solution: parse via `extractValueData` with `??`-chaining. (b) `0` is a valid minimum — never gate on `!n` / falsy zero-traps when `0` is semantically meaningful.
**Rejected alternatives**: Guidance-only (no proto, no server guard) — lost (no enforcement; agent `set_config` unguarded); Client-side (config-ui) bounds only — lost (stale clients and agent bypass it; C-10(c) fail-open trap); Mirror the string-only `platform.trading_state` guard — lost (round-3 fail-open: agent sends `float_val`, string-only coerces to `''`→`0`→inside `[0,8760]`→persists unchecked); Keep FLOAT_MAP machinery dormant after removing its sole key — lost (fails.md/074 pattern; `FLOAT_MAP = 1` member retained `[deprecated]` for wire stability); Enforce the decay bound at the analysis reader — lost (spreads bound into consumer, loses write-time rejection-at-source, doesn't populate config-ui bounds hint); Reuse FLOAT_MAP to render scalar bounds — lost (semantic lie; config-ui would render a map editor for a scalar value).
**Scars & gotchas**: (1) `listKeys` registry full-path mismatch (latent bug, now fixed): the existing `WEIGHT_KEY_REGISTRY[r.key]` pattern used the bare DB `key` column as the lookup key, but the registry was indexed by the full path (`analysis.scoring.signal_decay_half_life_hours`). This silently returned `undefined` in production from feature 016 onward — masked by a non-representative test fixture. Fix: index with `` `${namespace}.${r.key}` ``. (2) Round-3 fail-open catch: `Number(extractValueData(value))` with `??`-chaining correctly handles `float_val: 0` (`'0'`, no zero-trap); the string-only mirror would coerce the agent's `float_val` to `''`→`0`→inside `[0,8760]`→persist unchecked. (3) Docker codegen unavailable at execution time: fell back to host-toolchain path (`docs/runbooks/codegen-toolchain-host-setup.md`). (4) E2E dev-server cold-compile flakiness: resolved by running against a prebuilt prod server (`E2E_PREBUILT=1`) with `PLAYWRIGHT_CHROMIUM_EXECUTABLE_PATH` pointing to the pre-installed Chromium at `/opt/pw-browsers/chromium-1194`. (5) Stale `_SOURCES` fixture in `test_tools.py`: agent's existing fixture did not carry `reliability_weight` — updated as discovered side task during Steps 1-2. (6) 020 down-restore clobbers live operator edits: the down migration re-inserts the dead key's rows with hardcoded `value_data='{}'`, overwriting any live operator edit. (7) `/context-scrubber` plugin unavailable at execution: manual scrub performed instead.
**Permanent deviations**: Product-spec "no proto changes" → shipped `config.v1.ValueType.VALUE_TYPE_FLOAT_SCALAR = 2` — operator chose enforced bounds (Fork-1 sign-off, C-11 override); implementation-spec.md Step 6 Instruction 3 said bare-key lookup → shipped full-path lookup — sdd-review impl-spec caught the latent full-path/bare-key mismatch before execution; design assumed sdd-execute per-step feature-branch/PR loop → shipped via direct implementation on harness-assigned branch in one integration PR to `main-dev` — harness session model constraint; TDD red-green still applied per step.
**Cross-feature signal**: The `listKeys` registry lookup pattern (bare-key index vs full `namespace.key`) is a structural trap in `configServiceImpl.ts` that any future registry following the old `WEIGHT_KEY_REGISTRY` shape will inherit. The existing pattern was silently broken in production from feature 016 forward. Any future bounded config key must index its registry by full `namespace.key` and test with the split/DB-form fixture. Feature 022's deferred seed-migration is now complete.
**Deferred follow-ons**: The `8760` (1yr in hours) upper bound is a unit-typo guard; if an operator legitimately needs a >1yr half-life, the `SCALAR_BOUNDS_REGISTRY` ceiling and the proto `ValidationRule` doc-comment need updating. The defensive branch for negative values at `servicer.py:3248` is now unreachable via `SetConfig` (min=0) but remains for direct-DB edits; a future cleanup could remove it with a comment.
**Ledger entries written**: insights.md (2), fails.md (2) — see the 2026-08-31 entries.
**Runtime-invariant recommendations (→ /context-constitution)**: (1) CONFIG-*: The `listKeys` bare-key vs full-path registry lookup is a structural property of `configServiceImpl.ts`'s `listKeys` loop — `r.key` is the raw DB column value (namespace-stripped). Any future registry in this file must key by `` `${namespace}.${r.key}` ``. Candidate for `services/xstockstrat-config/docs/` context-constitution entry. (2) AGENT-*: The agent's `extractValueData`-style float write (sending `float_val`, not `string_val`) is the canonical form for all agent numeric `set_config` calls (`client.py:1572-1573`). Any server-side guard on a float-typed config key must parse via the all-shape path, not string-only. Candidate for `services/xstockstrat-agent/docs/` or `services/xstockstrat-config/docs/` context-constitution entry.
**Pruned artifacts**: product-spec.md, recon.md, design.md, implementation-spec.md — last present at 4903e98d2c9e7349ff0a80f90d0c0706fd7fc64f.

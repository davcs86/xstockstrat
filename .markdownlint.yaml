default: true
MD013: false  # line-length — markdown tables make this impossible to satisfy
MD033: false  # inline HTML — used for tables in root CLAUDE.md
MD041: false  # first-line H1 — CLAUDE.md files open with section headings, not a document title

# .env.fe.local — Frontend-only local dev overrides. Safe to commit.
# Loaded by docker-compose only on the 3 Next.js frontend services.
# Not loaded by backend services or Next.js directly (use .env.local for that).
APP_URL=http://localhost

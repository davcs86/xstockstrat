# Context: <slug>  (archived <ISO date>)

**Feature**: ./feature.md
**Status**: <terminal-status> — archived by /sdd-archiver; verbose specs pruned (recoverable via git history).

## Archive Synthesis — <ISO date> — /sdd-archiver

**What**: <2–4 sentences framed by the shipped outcome, not the requirement list>
**Why (irrecoverable rationale)**: <the decisive reasoning absent from code and specs>
**Rejected alternatives**: <alternative — lost because reason; one per line>
**Scars & gotchas**: <non-obvious traps found only by doing, from execute/post-launch sessions>
**Permanent deviations**: <design said X → shipped Y → because Z; one per line>
**Cross-feature signal**: <pattern linking to other features; or "none">
**Deferred follow-ons**: <forward pointers the feature spawned; or "none">
**Ledger entries written**: insights.md (<n>), fails.md (<m>) — see the <ISO date> entries.
**Runtime-invariant recommendations (→ /context-constitution)**: <PLAT-*/<MODULE>-* candidates; or "none">
**Pruned artifacts**: product-spec.md[, recon.md][, design.md], implementation-spec.md — last present at <pre-archive SHA>.
**Non-standard artifacts (human-decided at extras gate)**: <name> → <kept | relocated to X | deleted> per human choice on <ISO date>.  <!-- omit this line if the feature had no extras -->

// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.7
//   protoc               unknown
// source: config/v1/config.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import {
  type CallOptions,
  type ChannelCredentials,
  Client,
  type ClientOptions,
  type ClientReadableStream,
  type ClientUnaryCall,
  type handleServerStreamingCall,
  type handleUnaryCall,
  makeGenericClientConstructor,
  type Metadata,
  type ServiceError,
  type UntypedServiceImplementation,
} from "@grpc/grpc-js";
import {
  Environment,
  environmentFromJSON,
  environmentToJSON,
  environmentToNumber,
  TradingMode,
  tradingModeFromJSON,
  tradingModeToJSON,
  tradingModeToNumber,
} from "../../common/v1/common";
import { Struct } from "../../google/protobuf/struct";
import { Timestamp } from "../../google/protobuf/timestamp";

export const protobufPackage = "xstockstrat.config.v1";

export enum ConfigUpdateType {
  CONFIG_UPDATE_TYPE_UNSPECIFIED = "CONFIG_UPDATE_TYPE_UNSPECIFIED",
  /** CONFIG_UPDATE_TYPE_SNAPSHOT - full config dump */
  CONFIG_UPDATE_TYPE_SNAPSHOT = "CONFIG_UPDATE_TYPE_SNAPSHOT",
  /** CONFIG_UPDATE_TYPE_DELTA - only changed_keys changed */
  CONFIG_UPDATE_TYPE_DELTA = "CONFIG_UPDATE_TYPE_DELTA",
  /** CONFIG_UPDATE_TYPE_RELOAD - force full reload signal */
  CONFIG_UPDATE_TYPE_RELOAD = "CONFIG_UPDATE_TYPE_RELOAD",
  UNRECOGNIZED = "UNRECOGNIZED",
}

export function configUpdateTypeFromJSON(object: any): ConfigUpdateType {
  switch (object) {
    case 0:
    case "CONFIG_UPDATE_TYPE_UNSPECIFIED":
      return ConfigUpdateType.CONFIG_UPDATE_TYPE_UNSPECIFIED;
    case 1:
    case "CONFIG_UPDATE_TYPE_SNAPSHOT":
      return ConfigUpdateType.CONFIG_UPDATE_TYPE_SNAPSHOT;
    case 2:
    case "CONFIG_UPDATE_TYPE_DELTA":
      return ConfigUpdateType.CONFIG_UPDATE_TYPE_DELTA;
    case 3:
    case "CONFIG_UPDATE_TYPE_RELOAD":
      return ConfigUpdateType.CONFIG_UPDATE_TYPE_RELOAD;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ConfigUpdateType.UNRECOGNIZED;
  }
}

export function configUpdateTypeToJSON(object: ConfigUpdateType): string {
  switch (object) {
    case ConfigUpdateType.CONFIG_UPDATE_TYPE_UNSPECIFIED:
      return "CONFIG_UPDATE_TYPE_UNSPECIFIED";
    case ConfigUpdateType.CONFIG_UPDATE_TYPE_SNAPSHOT:
      return "CONFIG_UPDATE_TYPE_SNAPSHOT";
    case ConfigUpdateType.CONFIG_UPDATE_TYPE_DELTA:
      return "CONFIG_UPDATE_TYPE_DELTA";
    case ConfigUpdateType.CONFIG_UPDATE_TYPE_RELOAD:
      return "CONFIG_UPDATE_TYPE_RELOAD";
    case ConfigUpdateType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export function configUpdateTypeToNumber(object: ConfigUpdateType): number {
  switch (object) {
    case ConfigUpdateType.CONFIG_UPDATE_TYPE_UNSPECIFIED:
      return 0;
    case ConfigUpdateType.CONFIG_UPDATE_TYPE_SNAPSHOT:
      return 1;
    case ConfigUpdateType.CONFIG_UPDATE_TYPE_DELTA:
      return 2;
    case ConfigUpdateType.CONFIG_UPDATE_TYPE_RELOAD:
      return 3;
    case ConfigUpdateType.UNRECOGNIZED:
    default:
      return -1;
  }
}

export interface WatchConfigRequest {
  /** e.g. "indicators", "trading", "platform" */
  namespace: string;
  /** service instance identifier */
  clientId: string;
  /** last known version (for delta updates) */
  version: string;
  /** dev or production; defaults to dev */
  environment: Environment;
  /** paper or live; 'all' rows included always */
  tradingMode: TradingMode;
}

export interface ConfigSnapshot {
  namespace: string;
  version: string;
  updatedAt?: Date | undefined;
  values: { [key: string]: ConfigValue };
  updateType: ConfigUpdateType;
  /** populated for DELTA updates */
  changedKeys: string[];
  environment: Environment;
  tradingMode: TradingMode;
}

export interface ConfigSnapshot_ValuesEntry {
  key: string;
  value?: ConfigValue | undefined;
}

export interface ConfigValue {
  stringVal?: string | undefined;
  intVal?: number | undefined;
  floatVal?: number | undefined;
  boolVal?: boolean | undefined;
  jsonVal?:
    | { [key: string]: any }
    | undefined;
  /** true = value is redacted; resolved at runtime */
  isSecret: boolean;
  description: string;
  defaultValue: string;
}

export interface GetConfigRequest {
  namespace: string;
  environment: Environment;
  tradingMode: TradingMode;
}

export interface SetConfigRequest {
  namespace: string;
  key: string;
  value?: ConfigValue | undefined;
  author: string;
  reason: string;
  environment: Environment;
  tradingMode: TradingMode;
}

export interface SetConfigResponse {
  version: string;
  updatedAt?: Date | undefined;
}

export interface ListKeysRequest {
  namespace: string;
  environment: Environment;
  tradingMode: TradingMode;
}

export interface ListKeysResponse {
  keys: ConfigKeyMeta[];
}

export interface ConfigKeyMeta {
  key: string;
  description: string;
  defaultValue: string;
  isSecret: boolean;
  consumingService: string;
  environment: Environment;
  tradingMode: TradingMode;
}

function createBaseWatchConfigRequest(): WatchConfigRequest {
  return {
    namespace: "",
    clientId: "",
    version: "",
    environment: Environment.ENVIRONMENT_UNSPECIFIED,
    tradingMode: TradingMode.TRADING_MODE_UNSPECIFIED,
  };
}

export const WatchConfigRequest: MessageFns<WatchConfigRequest> = {
  encode(message: WatchConfigRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.namespace !== "") {
      writer.uint32(10).string(message.namespace);
    }
    if (message.clientId !== "") {
      writer.uint32(18).string(message.clientId);
    }
    if (message.version !== "") {
      writer.uint32(26).string(message.version);
    }
    if (message.environment !== Environment.ENVIRONMENT_UNSPECIFIED) {
      writer.uint32(32).int32(environmentToNumber(message.environment));
    }
    if (message.tradingMode !== TradingMode.TRADING_MODE_UNSPECIFIED) {
      writer.uint32(40).int32(tradingModeToNumber(message.tradingMode));
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WatchConfigRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWatchConfigRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.namespace = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.clientId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.version = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.environment = environmentFromJSON(reader.int32());
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.tradingMode = tradingModeFromJSON(reader.int32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WatchConfigRequest {
    return {
      namespace: isSet(object.namespace) ? globalThis.String(object.namespace) : "",
      clientId: isSet(object.clientId)
        ? globalThis.String(object.clientId)
        : isSet(object.client_id)
        ? globalThis.String(object.client_id)
        : "",
      version: isSet(object.version) ? globalThis.String(object.version) : "",
      environment: isSet(object.environment)
        ? environmentFromJSON(object.environment)
        : Environment.ENVIRONMENT_UNSPECIFIED,
      tradingMode: isSet(object.tradingMode)
        ? tradingModeFromJSON(object.tradingMode)
        : isSet(object.trading_mode)
        ? tradingModeFromJSON(object.trading_mode)
        : TradingMode.TRADING_MODE_UNSPECIFIED,
    };
  },

  toJSON(message: WatchConfigRequest): unknown {
    const obj: any = {};
    if (message.namespace !== "") {
      obj.namespace = message.namespace;
    }
    if (message.clientId !== "") {
      obj.clientId = message.clientId;
    }
    if (message.version !== "") {
      obj.version = message.version;
    }
    if (message.environment !== Environment.ENVIRONMENT_UNSPECIFIED) {
      obj.environment = environmentToJSON(message.environment);
    }
    if (message.tradingMode !== TradingMode.TRADING_MODE_UNSPECIFIED) {
      obj.tradingMode = tradingModeToJSON(message.tradingMode);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<WatchConfigRequest>, I>>(base?: I): WatchConfigRequest {
    return WatchConfigRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<WatchConfigRequest>, I>>(object: I): WatchConfigRequest {
    const message = createBaseWatchConfigRequest();
    message.namespace = object.namespace ?? "";
    message.clientId = object.clientId ?? "";
    message.version = object.version ?? "";
    message.environment = object.environment ?? Environment.ENVIRONMENT_UNSPECIFIED;
    message.tradingMode = object.tradingMode ?? TradingMode.TRADING_MODE_UNSPECIFIED;
    return message;
  },
};

function createBaseConfigSnapshot(): ConfigSnapshot {
  return {
    namespace: "",
    version: "",
    updatedAt: undefined,
    values: {},
    updateType: ConfigUpdateType.CONFIG_UPDATE_TYPE_UNSPECIFIED,
    changedKeys: [],
    environment: Environment.ENVIRONMENT_UNSPECIFIED,
    tradingMode: TradingMode.TRADING_MODE_UNSPECIFIED,
  };
}

export const ConfigSnapshot: MessageFns<ConfigSnapshot> = {
  encode(message: ConfigSnapshot, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.namespace !== "") {
      writer.uint32(10).string(message.namespace);
    }
    if (message.version !== "") {
      writer.uint32(18).string(message.version);
    }
    if (message.updatedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.updatedAt), writer.uint32(26).fork()).join();
    }
    globalThis.Object.entries(message.values).forEach(([key, value]: [string, ConfigValue]) => {
      ConfigSnapshot_ValuesEntry.encode({ key: key as any, value }, writer.uint32(34).fork()).join();
    });
    if (message.updateType !== ConfigUpdateType.CONFIG_UPDATE_TYPE_UNSPECIFIED) {
      writer.uint32(40).int32(configUpdateTypeToNumber(message.updateType));
    }
    for (const v of message.changedKeys) {
      writer.uint32(50).string(v!);
    }
    if (message.environment !== Environment.ENVIRONMENT_UNSPECIFIED) {
      writer.uint32(56).int32(environmentToNumber(message.environment));
    }
    if (message.tradingMode !== TradingMode.TRADING_MODE_UNSPECIFIED) {
      writer.uint32(64).int32(tradingModeToNumber(message.tradingMode));
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ConfigSnapshot {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseConfigSnapshot();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.namespace = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.version = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.updatedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          const entry4 = ConfigSnapshot_ValuesEntry.decode(reader, reader.uint32());
          if (entry4.value !== undefined) {
            message.values[entry4.key] = entry4.value;
          }
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.updateType = configUpdateTypeFromJSON(reader.int32());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.changedKeys.push(reader.string());
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.environment = environmentFromJSON(reader.int32());
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.tradingMode = tradingModeFromJSON(reader.int32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ConfigSnapshot {
    return {
      namespace: isSet(object.namespace) ? globalThis.String(object.namespace) : "",
      version: isSet(object.version) ? globalThis.String(object.version) : "",
      updatedAt: isSet(object.updatedAt)
        ? fromJsonTimestamp(object.updatedAt)
        : isSet(object.updated_at)
        ? fromJsonTimestamp(object.updated_at)
        : undefined,
      values: isObject(object.values)
        ? (globalThis.Object.entries(object.values) as [string, any][]).reduce(
          (acc: { [key: string]: ConfigValue }, [key, value]: [string, any]) => {
            acc[key] = ConfigValue.fromJSON(value);
            return acc;
          },
          {},
        )
        : {},
      updateType: isSet(object.updateType)
        ? configUpdateTypeFromJSON(object.updateType)
        : isSet(object.update_type)
        ? configUpdateTypeFromJSON(object.update_type)
        : ConfigUpdateType.CONFIG_UPDATE_TYPE_UNSPECIFIED,
      changedKeys: globalThis.Array.isArray(object?.changedKeys)
        ? object.changedKeys.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.changed_keys)
        ? object.changed_keys.map((e: any) => globalThis.String(e))
        : [],
      environment: isSet(object.environment)
        ? environmentFromJSON(object.environment)
        : Environment.ENVIRONMENT_UNSPECIFIED,
      tradingMode: isSet(object.tradingMode)
        ? tradingModeFromJSON(object.tradingMode)
        : isSet(object.trading_mode)
        ? tradingModeFromJSON(object.trading_mode)
        : TradingMode.TRADING_MODE_UNSPECIFIED,
    };
  },

  toJSON(message: ConfigSnapshot): unknown {
    const obj: any = {};
    if (message.namespace !== "") {
      obj.namespace = message.namespace;
    }
    if (message.version !== "") {
      obj.version = message.version;
    }
    if (message.updatedAt !== undefined) {
      obj.updatedAt = message.updatedAt.toISOString();
    }
    if (message.values) {
      const entries = globalThis.Object.entries(message.values) as [string, ConfigValue][];
      if (entries.length > 0) {
        obj.values = {};
        entries.forEach(([k, v]) => {
          obj.values[k] = ConfigValue.toJSON(v);
        });
      }
    }
    if (message.updateType !== ConfigUpdateType.CONFIG_UPDATE_TYPE_UNSPECIFIED) {
      obj.updateType = configUpdateTypeToJSON(message.updateType);
    }
    if (message.changedKeys?.length) {
      obj.changedKeys = message.changedKeys;
    }
    if (message.environment !== Environment.ENVIRONMENT_UNSPECIFIED) {
      obj.environment = environmentToJSON(message.environment);
    }
    if (message.tradingMode !== TradingMode.TRADING_MODE_UNSPECIFIED) {
      obj.tradingMode = tradingModeToJSON(message.tradingMode);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ConfigSnapshot>, I>>(base?: I): ConfigSnapshot {
    return ConfigSnapshot.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ConfigSnapshot>, I>>(object: I): ConfigSnapshot {
    const message = createBaseConfigSnapshot();
    message.namespace = object.namespace ?? "";
    message.version = object.version ?? "";
    message.updatedAt = object.updatedAt ?? undefined;
    message.values = (globalThis.Object.entries(object.values ?? {}) as [string, ConfigValue][]).reduce(
      (acc: { [key: string]: ConfigValue }, [key, value]: [string, ConfigValue]) => {
        if (value !== undefined) {
          acc[key] = ConfigValue.fromPartial(value);
        }
        return acc;
      },
      {},
    );
    message.updateType = object.updateType ?? ConfigUpdateType.CONFIG_UPDATE_TYPE_UNSPECIFIED;
    message.changedKeys = object.changedKeys?.map((e) => e) || [];
    message.environment = object.environment ?? Environment.ENVIRONMENT_UNSPECIFIED;
    message.tradingMode = object.tradingMode ?? TradingMode.TRADING_MODE_UNSPECIFIED;
    return message;
  },
};

function createBaseConfigSnapshot_ValuesEntry(): ConfigSnapshot_ValuesEntry {
  return { key: "", value: undefined };
}

export const ConfigSnapshot_ValuesEntry: MessageFns<ConfigSnapshot_ValuesEntry> = {
  encode(message: ConfigSnapshot_ValuesEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== undefined) {
      ConfigValue.encode(message.value, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ConfigSnapshot_ValuesEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseConfigSnapshot_ValuesEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = ConfigValue.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ConfigSnapshot_ValuesEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? ConfigValue.fromJSON(object.value) : undefined,
    };
  },

  toJSON(message: ConfigSnapshot_ValuesEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== undefined) {
      obj.value = ConfigValue.toJSON(message.value);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ConfigSnapshot_ValuesEntry>, I>>(base?: I): ConfigSnapshot_ValuesEntry {
    return ConfigSnapshot_ValuesEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ConfigSnapshot_ValuesEntry>, I>>(object: I): ConfigSnapshot_ValuesEntry {
    const message = createBaseConfigSnapshot_ValuesEntry();
    message.key = object.key ?? "";
    message.value = (object.value !== undefined && object.value !== null)
      ? ConfigValue.fromPartial(object.value)
      : undefined;
    return message;
  },
};

function createBaseConfigValue(): ConfigValue {
  return {
    stringVal: undefined,
    intVal: undefined,
    floatVal: undefined,
    boolVal: undefined,
    jsonVal: undefined,
    isSecret: false,
    description: "",
    defaultValue: "",
  };
}

export const ConfigValue: MessageFns<ConfigValue> = {
  encode(message: ConfigValue, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.stringVal !== undefined) {
      writer.uint32(10).string(message.stringVal);
    }
    if (message.intVal !== undefined) {
      writer.uint32(16).int64(message.intVal);
    }
    if (message.floatVal !== undefined) {
      writer.uint32(25).double(message.floatVal);
    }
    if (message.boolVal !== undefined) {
      writer.uint32(32).bool(message.boolVal);
    }
    if (message.jsonVal !== undefined) {
      Struct.encode(Struct.wrap(message.jsonVal), writer.uint32(42).fork()).join();
    }
    if (message.isSecret !== false) {
      writer.uint32(48).bool(message.isSecret);
    }
    if (message.description !== "") {
      writer.uint32(58).string(message.description);
    }
    if (message.defaultValue !== "") {
      writer.uint32(66).string(message.defaultValue);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ConfigValue {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseConfigValue();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.stringVal = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.intVal = longToNumber(reader.int64());
          continue;
        }
        case 3: {
          if (tag !== 25) {
            break;
          }

          message.floatVal = reader.double();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.boolVal = reader.bool();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.jsonVal = Struct.unwrap(Struct.decode(reader, reader.uint32()));
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.isSecret = reader.bool();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.defaultValue = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ConfigValue {
    return {
      stringVal: isSet(object.stringVal)
        ? globalThis.String(object.stringVal)
        : isSet(object.string_val)
        ? globalThis.String(object.string_val)
        : undefined,
      intVal: isSet(object.intVal)
        ? globalThis.Number(object.intVal)
        : isSet(object.int_val)
        ? globalThis.Number(object.int_val)
        : undefined,
      floatVal: isSet(object.floatVal)
        ? globalThis.Number(object.floatVal)
        : isSet(object.float_val)
        ? globalThis.Number(object.float_val)
        : undefined,
      boolVal: isSet(object.boolVal)
        ? globalThis.Boolean(object.boolVal)
        : isSet(object.bool_val)
        ? globalThis.Boolean(object.bool_val)
        : undefined,
      jsonVal: isObject(object.jsonVal) ? object.jsonVal : isObject(object.json_val) ? object.json_val : undefined,
      isSecret: isSet(object.isSecret)
        ? globalThis.Boolean(object.isSecret)
        : isSet(object.is_secret)
        ? globalThis.Boolean(object.is_secret)
        : false,
      description: isSet(object.description) ? globalThis.String(object.description) : "",
      defaultValue: isSet(object.defaultValue)
        ? globalThis.String(object.defaultValue)
        : isSet(object.default_value)
        ? globalThis.String(object.default_value)
        : "",
    };
  },

  toJSON(message: ConfigValue): unknown {
    const obj: any = {};
    if (message.stringVal !== undefined) {
      obj.stringVal = message.stringVal;
    }
    if (message.intVal !== undefined) {
      obj.intVal = Math.round(message.intVal);
    }
    if (message.floatVal !== undefined) {
      obj.floatVal = message.floatVal;
    }
    if (message.boolVal !== undefined) {
      obj.boolVal = message.boolVal;
    }
    if (message.jsonVal !== undefined) {
      obj.jsonVal = message.jsonVal;
    }
    if (message.isSecret !== false) {
      obj.isSecret = message.isSecret;
    }
    if (message.description !== "") {
      obj.description = message.description;
    }
    if (message.defaultValue !== "") {
      obj.defaultValue = message.defaultValue;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ConfigValue>, I>>(base?: I): ConfigValue {
    return ConfigValue.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ConfigValue>, I>>(object: I): ConfigValue {
    const message = createBaseConfigValue();
    message.stringVal = object.stringVal ?? undefined;
    message.intVal = object.intVal ?? undefined;
    message.floatVal = object.floatVal ?? undefined;
    message.boolVal = object.boolVal ?? undefined;
    message.jsonVal = object.jsonVal ?? undefined;
    message.isSecret = object.isSecret ?? false;
    message.description = object.description ?? "";
    message.defaultValue = object.defaultValue ?? "";
    return message;
  },
};

function createBaseGetConfigRequest(): GetConfigRequest {
  return {
    namespace: "",
    environment: Environment.ENVIRONMENT_UNSPECIFIED,
    tradingMode: TradingMode.TRADING_MODE_UNSPECIFIED,
  };
}

export const GetConfigRequest: MessageFns<GetConfigRequest> = {
  encode(message: GetConfigRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.namespace !== "") {
      writer.uint32(10).string(message.namespace);
    }
    if (message.environment !== Environment.ENVIRONMENT_UNSPECIFIED) {
      writer.uint32(16).int32(environmentToNumber(message.environment));
    }
    if (message.tradingMode !== TradingMode.TRADING_MODE_UNSPECIFIED) {
      writer.uint32(24).int32(tradingModeToNumber(message.tradingMode));
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetConfigRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetConfigRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.namespace = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.environment = environmentFromJSON(reader.int32());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.tradingMode = tradingModeFromJSON(reader.int32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetConfigRequest {
    return {
      namespace: isSet(object.namespace) ? globalThis.String(object.namespace) : "",
      environment: isSet(object.environment)
        ? environmentFromJSON(object.environment)
        : Environment.ENVIRONMENT_UNSPECIFIED,
      tradingMode: isSet(object.tradingMode)
        ? tradingModeFromJSON(object.tradingMode)
        : isSet(object.trading_mode)
        ? tradingModeFromJSON(object.trading_mode)
        : TradingMode.TRADING_MODE_UNSPECIFIED,
    };
  },

  toJSON(message: GetConfigRequest): unknown {
    const obj: any = {};
    if (message.namespace !== "") {
      obj.namespace = message.namespace;
    }
    if (message.environment !== Environment.ENVIRONMENT_UNSPECIFIED) {
      obj.environment = environmentToJSON(message.environment);
    }
    if (message.tradingMode !== TradingMode.TRADING_MODE_UNSPECIFIED) {
      obj.tradingMode = tradingModeToJSON(message.tradingMode);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetConfigRequest>, I>>(base?: I): GetConfigRequest {
    return GetConfigRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetConfigRequest>, I>>(object: I): GetConfigRequest {
    const message = createBaseGetConfigRequest();
    message.namespace = object.namespace ?? "";
    message.environment = object.environment ?? Environment.ENVIRONMENT_UNSPECIFIED;
    message.tradingMode = object.tradingMode ?? TradingMode.TRADING_MODE_UNSPECIFIED;
    return message;
  },
};

function createBaseSetConfigRequest(): SetConfigRequest {
  return {
    namespace: "",
    key: "",
    value: undefined,
    author: "",
    reason: "",
    environment: Environment.ENVIRONMENT_UNSPECIFIED,
    tradingMode: TradingMode.TRADING_MODE_UNSPECIFIED,
  };
}

export const SetConfigRequest: MessageFns<SetConfigRequest> = {
  encode(message: SetConfigRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.namespace !== "") {
      writer.uint32(10).string(message.namespace);
    }
    if (message.key !== "") {
      writer.uint32(18).string(message.key);
    }
    if (message.value !== undefined) {
      ConfigValue.encode(message.value, writer.uint32(26).fork()).join();
    }
    if (message.author !== "") {
      writer.uint32(34).string(message.author);
    }
    if (message.reason !== "") {
      writer.uint32(42).string(message.reason);
    }
    if (message.environment !== Environment.ENVIRONMENT_UNSPECIFIED) {
      writer.uint32(48).int32(environmentToNumber(message.environment));
    }
    if (message.tradingMode !== TradingMode.TRADING_MODE_UNSPECIFIED) {
      writer.uint32(56).int32(tradingModeToNumber(message.tradingMode));
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SetConfigRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSetConfigRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.namespace = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.value = ConfigValue.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.author = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.reason = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.environment = environmentFromJSON(reader.int32());
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.tradingMode = tradingModeFromJSON(reader.int32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SetConfigRequest {
    return {
      namespace: isSet(object.namespace) ? globalThis.String(object.namespace) : "",
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? ConfigValue.fromJSON(object.value) : undefined,
      author: isSet(object.author) ? globalThis.String(object.author) : "",
      reason: isSet(object.reason) ? globalThis.String(object.reason) : "",
      environment: isSet(object.environment)
        ? environmentFromJSON(object.environment)
        : Environment.ENVIRONMENT_UNSPECIFIED,
      tradingMode: isSet(object.tradingMode)
        ? tradingModeFromJSON(object.tradingMode)
        : isSet(object.trading_mode)
        ? tradingModeFromJSON(object.trading_mode)
        : TradingMode.TRADING_MODE_UNSPECIFIED,
    };
  },

  toJSON(message: SetConfigRequest): unknown {
    const obj: any = {};
    if (message.namespace !== "") {
      obj.namespace = message.namespace;
    }
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== undefined) {
      obj.value = ConfigValue.toJSON(message.value);
    }
    if (message.author !== "") {
      obj.author = message.author;
    }
    if (message.reason !== "") {
      obj.reason = message.reason;
    }
    if (message.environment !== Environment.ENVIRONMENT_UNSPECIFIED) {
      obj.environment = environmentToJSON(message.environment);
    }
    if (message.tradingMode !== TradingMode.TRADING_MODE_UNSPECIFIED) {
      obj.tradingMode = tradingModeToJSON(message.tradingMode);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SetConfigRequest>, I>>(base?: I): SetConfigRequest {
    return SetConfigRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SetConfigRequest>, I>>(object: I): SetConfigRequest {
    const message = createBaseSetConfigRequest();
    message.namespace = object.namespace ?? "";
    message.key = object.key ?? "";
    message.value = (object.value !== undefined && object.value !== null)
      ? ConfigValue.fromPartial(object.value)
      : undefined;
    message.author = object.author ?? "";
    message.reason = object.reason ?? "";
    message.environment = object.environment ?? Environment.ENVIRONMENT_UNSPECIFIED;
    message.tradingMode = object.tradingMode ?? TradingMode.TRADING_MODE_UNSPECIFIED;
    return message;
  },
};

function createBaseSetConfigResponse(): SetConfigResponse {
  return { version: "", updatedAt: undefined };
}

export const SetConfigResponse: MessageFns<SetConfigResponse> = {
  encode(message: SetConfigResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.version !== "") {
      writer.uint32(10).string(message.version);
    }
    if (message.updatedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.updatedAt), writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SetConfigResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSetConfigResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.version = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.updatedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SetConfigResponse {
    return {
      version: isSet(object.version) ? globalThis.String(object.version) : "",
      updatedAt: isSet(object.updatedAt)
        ? fromJsonTimestamp(object.updatedAt)
        : isSet(object.updated_at)
        ? fromJsonTimestamp(object.updated_at)
        : undefined,
    };
  },

  toJSON(message: SetConfigResponse): unknown {
    const obj: any = {};
    if (message.version !== "") {
      obj.version = message.version;
    }
    if (message.updatedAt !== undefined) {
      obj.updatedAt = message.updatedAt.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SetConfigResponse>, I>>(base?: I): SetConfigResponse {
    return SetConfigResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SetConfigResponse>, I>>(object: I): SetConfigResponse {
    const message = createBaseSetConfigResponse();
    message.version = object.version ?? "";
    message.updatedAt = object.updatedAt ?? undefined;
    return message;
  },
};

function createBaseListKeysRequest(): ListKeysRequest {
  return {
    namespace: "",
    environment: Environment.ENVIRONMENT_UNSPECIFIED,
    tradingMode: TradingMode.TRADING_MODE_UNSPECIFIED,
  };
}

export const ListKeysRequest: MessageFns<ListKeysRequest> = {
  encode(message: ListKeysRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.namespace !== "") {
      writer.uint32(10).string(message.namespace);
    }
    if (message.environment !== Environment.ENVIRONMENT_UNSPECIFIED) {
      writer.uint32(16).int32(environmentToNumber(message.environment));
    }
    if (message.tradingMode !== TradingMode.TRADING_MODE_UNSPECIFIED) {
      writer.uint32(24).int32(tradingModeToNumber(message.tradingMode));
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListKeysRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListKeysRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.namespace = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.environment = environmentFromJSON(reader.int32());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.tradingMode = tradingModeFromJSON(reader.int32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListKeysRequest {
    return {
      namespace: isSet(object.namespace) ? globalThis.String(object.namespace) : "",
      environment: isSet(object.environment)
        ? environmentFromJSON(object.environment)
        : Environment.ENVIRONMENT_UNSPECIFIED,
      tradingMode: isSet(object.tradingMode)
        ? tradingModeFromJSON(object.tradingMode)
        : isSet(object.trading_mode)
        ? tradingModeFromJSON(object.trading_mode)
        : TradingMode.TRADING_MODE_UNSPECIFIED,
    };
  },

  toJSON(message: ListKeysRequest): unknown {
    const obj: any = {};
    if (message.namespace !== "") {
      obj.namespace = message.namespace;
    }
    if (message.environment !== Environment.ENVIRONMENT_UNSPECIFIED) {
      obj.environment = environmentToJSON(message.environment);
    }
    if (message.tradingMode !== TradingMode.TRADING_MODE_UNSPECIFIED) {
      obj.tradingMode = tradingModeToJSON(message.tradingMode);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListKeysRequest>, I>>(base?: I): ListKeysRequest {
    return ListKeysRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListKeysRequest>, I>>(object: I): ListKeysRequest {
    const message = createBaseListKeysRequest();
    message.namespace = object.namespace ?? "";
    message.environment = object.environment ?? Environment.ENVIRONMENT_UNSPECIFIED;
    message.tradingMode = object.tradingMode ?? TradingMode.TRADING_MODE_UNSPECIFIED;
    return message;
  },
};

function createBaseListKeysResponse(): ListKeysResponse {
  return { keys: [] };
}

export const ListKeysResponse: MessageFns<ListKeysResponse> = {
  encode(message: ListKeysResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.keys) {
      ConfigKeyMeta.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListKeysResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListKeysResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.keys.push(ConfigKeyMeta.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListKeysResponse {
    return {
      keys: globalThis.Array.isArray(object?.keys) ? object.keys.map((e: any) => ConfigKeyMeta.fromJSON(e)) : [],
    };
  },

  toJSON(message: ListKeysResponse): unknown {
    const obj: any = {};
    if (message.keys?.length) {
      obj.keys = message.keys.map((e) => ConfigKeyMeta.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListKeysResponse>, I>>(base?: I): ListKeysResponse {
    return ListKeysResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListKeysResponse>, I>>(object: I): ListKeysResponse {
    const message = createBaseListKeysResponse();
    message.keys = object.keys?.map((e) => ConfigKeyMeta.fromPartial(e)) || [];
    return message;
  },
};

function createBaseConfigKeyMeta(): ConfigKeyMeta {
  return {
    key: "",
    description: "",
    defaultValue: "",
    isSecret: false,
    consumingService: "",
    environment: Environment.ENVIRONMENT_UNSPECIFIED,
    tradingMode: TradingMode.TRADING_MODE_UNSPECIFIED,
  };
}

export const ConfigKeyMeta: MessageFns<ConfigKeyMeta> = {
  encode(message: ConfigKeyMeta, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.description !== "") {
      writer.uint32(18).string(message.description);
    }
    if (message.defaultValue !== "") {
      writer.uint32(26).string(message.defaultValue);
    }
    if (message.isSecret !== false) {
      writer.uint32(32).bool(message.isSecret);
    }
    if (message.consumingService !== "") {
      writer.uint32(42).string(message.consumingService);
    }
    if (message.environment !== Environment.ENVIRONMENT_UNSPECIFIED) {
      writer.uint32(48).int32(environmentToNumber(message.environment));
    }
    if (message.tradingMode !== TradingMode.TRADING_MODE_UNSPECIFIED) {
      writer.uint32(56).int32(tradingModeToNumber(message.tradingMode));
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ConfigKeyMeta {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseConfigKeyMeta();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.defaultValue = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.isSecret = reader.bool();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.consumingService = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.environment = environmentFromJSON(reader.int32());
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.tradingMode = tradingModeFromJSON(reader.int32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ConfigKeyMeta {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      description: isSet(object.description) ? globalThis.String(object.description) : "",
      defaultValue: isSet(object.defaultValue)
        ? globalThis.String(object.defaultValue)
        : isSet(object.default_value)
        ? globalThis.String(object.default_value)
        : "",
      isSecret: isSet(object.isSecret)
        ? globalThis.Boolean(object.isSecret)
        : isSet(object.is_secret)
        ? globalThis.Boolean(object.is_secret)
        : false,
      consumingService: isSet(object.consumingService)
        ? globalThis.String(object.consumingService)
        : isSet(object.consuming_service)
        ? globalThis.String(object.consuming_service)
        : "",
      environment: isSet(object.environment)
        ? environmentFromJSON(object.environment)
        : Environment.ENVIRONMENT_UNSPECIFIED,
      tradingMode: isSet(object.tradingMode)
        ? tradingModeFromJSON(object.tradingMode)
        : isSet(object.trading_mode)
        ? tradingModeFromJSON(object.trading_mode)
        : TradingMode.TRADING_MODE_UNSPECIFIED,
    };
  },

  toJSON(message: ConfigKeyMeta): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.description !== "") {
      obj.description = message.description;
    }
    if (message.defaultValue !== "") {
      obj.defaultValue = message.defaultValue;
    }
    if (message.isSecret !== false) {
      obj.isSecret = message.isSecret;
    }
    if (message.consumingService !== "") {
      obj.consumingService = message.consumingService;
    }
    if (message.environment !== Environment.ENVIRONMENT_UNSPECIFIED) {
      obj.environment = environmentToJSON(message.environment);
    }
    if (message.tradingMode !== TradingMode.TRADING_MODE_UNSPECIFIED) {
      obj.tradingMode = tradingModeToJSON(message.tradingMode);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ConfigKeyMeta>, I>>(base?: I): ConfigKeyMeta {
    return ConfigKeyMeta.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ConfigKeyMeta>, I>>(object: I): ConfigKeyMeta {
    const message = createBaseConfigKeyMeta();
    message.key = object.key ?? "";
    message.description = object.description ?? "";
    message.defaultValue = object.defaultValue ?? "";
    message.isSecret = object.isSecret ?? false;
    message.consumingService = object.consumingService ?? "";
    message.environment = object.environment ?? Environment.ENVIRONMENT_UNSPECIFIED;
    message.tradingMode = object.tradingMode ?? TradingMode.TRADING_MODE_UNSPECIFIED;
    return message;
  },
};

/**
 * ConfigService — live configuration via server-streaming WatchConfig.
 * All services call WatchConfig at startup and stream config updates.
 * Config values are scoped by environment (dev/production) and trading_mode (paper/live/all).
 */
export type ConfigServiceService = typeof ConfigServiceService;
export const ConfigServiceService = {
  /**
   * Subscribe to config updates for a given namespace/service.
   * Server streams updates as config changes; initial snapshot is the first message.
   */
  watchConfig: {
    path: "/xstockstrat.config.v1.ConfigService/WatchConfig" as const,
    requestStream: false as const,
    responseStream: true as const,
    requestSerialize: (value: WatchConfigRequest): Buffer => Buffer.from(WatchConfigRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): WatchConfigRequest => WatchConfigRequest.decode(value),
    responseSerialize: (value: ConfigSnapshot): Buffer => Buffer.from(ConfigSnapshot.encode(value).finish()),
    responseDeserialize: (value: Buffer): ConfigSnapshot => ConfigSnapshot.decode(value),
  },
  /** One-shot config fetch (startup fast-path before stream establishes) */
  getConfig: {
    path: "/xstockstrat.config.v1.ConfigService/GetConfig" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: GetConfigRequest): Buffer => Buffer.from(GetConfigRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): GetConfigRequest => GetConfigRequest.decode(value),
    responseSerialize: (value: ConfigSnapshot): Buffer => Buffer.from(ConfigSnapshot.encode(value).finish()),
    responseDeserialize: (value: Buffer): ConfigSnapshot => ConfigSnapshot.decode(value),
  },
  /** Admin: set or update a config value */
  setConfig: {
    path: "/xstockstrat.config.v1.ConfigService/SetConfig" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: SetConfigRequest): Buffer => Buffer.from(SetConfigRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): SetConfigRequest => SetConfigRequest.decode(value),
    responseSerialize: (value: SetConfigResponse): Buffer => Buffer.from(SetConfigResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): SetConfigResponse => SetConfigResponse.decode(value),
  },
  /** Admin: list all keys for a namespace */
  listKeys: {
    path: "/xstockstrat.config.v1.ConfigService/ListKeys" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: ListKeysRequest): Buffer => Buffer.from(ListKeysRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): ListKeysRequest => ListKeysRequest.decode(value),
    responseSerialize: (value: ListKeysResponse): Buffer => Buffer.from(ListKeysResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): ListKeysResponse => ListKeysResponse.decode(value),
  },
} as const;

export interface ConfigServiceServer extends UntypedServiceImplementation {
  /**
   * Subscribe to config updates for a given namespace/service.
   * Server streams updates as config changes; initial snapshot is the first message.
   */
  watchConfig: handleServerStreamingCall<WatchConfigRequest, ConfigSnapshot>;
  /** One-shot config fetch (startup fast-path before stream establishes) */
  getConfig: handleUnaryCall<GetConfigRequest, ConfigSnapshot>;
  /** Admin: set or update a config value */
  setConfig: handleUnaryCall<SetConfigRequest, SetConfigResponse>;
  /** Admin: list all keys for a namespace */
  listKeys: handleUnaryCall<ListKeysRequest, ListKeysResponse>;
}

export interface ConfigServiceClient extends Client {
  /**
   * Subscribe to config updates for a given namespace/service.
   * Server streams updates as config changes; initial snapshot is the first message.
   */
  watchConfig(request: WatchConfigRequest, options?: Partial<CallOptions>): ClientReadableStream<ConfigSnapshot>;
  watchConfig(
    request: WatchConfigRequest,
    metadata?: Metadata,
    options?: Partial<CallOptions>,
  ): ClientReadableStream<ConfigSnapshot>;
  /** One-shot config fetch (startup fast-path before stream establishes) */
  getConfig(
    request: GetConfigRequest,
    callback: (error: ServiceError | null, response: ConfigSnapshot) => void,
  ): ClientUnaryCall;
  getConfig(
    request: GetConfigRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: ConfigSnapshot) => void,
  ): ClientUnaryCall;
  getConfig(
    request: GetConfigRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: ConfigSnapshot) => void,
  ): ClientUnaryCall;
  /** Admin: set or update a config value */
  setConfig(
    request: SetConfigRequest,
    callback: (error: ServiceError | null, response: SetConfigResponse) => void,
  ): ClientUnaryCall;
  setConfig(
    request: SetConfigRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: SetConfigResponse) => void,
  ): ClientUnaryCall;
  setConfig(
    request: SetConfigRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: SetConfigResponse) => void,
  ): ClientUnaryCall;
  /** Admin: list all keys for a namespace */
  listKeys(
    request: ListKeysRequest,
    callback: (error: ServiceError | null, response: ListKeysResponse) => void,
  ): ClientUnaryCall;
  listKeys(
    request: ListKeysRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: ListKeysResponse) => void,
  ): ClientUnaryCall;
  listKeys(
    request: ListKeysRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: ListKeysResponse) => void,
  ): ClientUnaryCall;
}

export const ConfigServiceClient = makeGenericClientConstructor(
  ConfigServiceService,
  "xstockstrat.config.v1.ConfigService",
) as unknown as {
  new (address: string, credentials: ChannelCredentials, options?: Partial<ClientOptions>): ConfigServiceClient;
  service: typeof ConfigServiceService;
  serviceName: string;
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}

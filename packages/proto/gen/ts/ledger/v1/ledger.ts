// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               unknown
// source: ledger/v1/ledger.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import {
  type CallOptions,
  type ChannelCredentials,
  Client,
  type ClientOptions,
  type ClientReadableStream,
  type ClientUnaryCall,
  type handleServerStreamingCall,
  type handleUnaryCall,
  makeGenericClientConstructor,
  type Metadata,
  type ServiceError,
  type UntypedServiceImplementation,
} from "@grpc/grpc-js";
import { PageRequest, PageResponse, TimeRange } from "../../common/v1/common";
import { Struct } from "../../google/protobuf/struct";
import { Timestamp } from "../../google/protobuf/timestamp";

export const protobufPackage = "xstockstrat.ledger.v1";

export interface LedgerEvent {
  /** UUID, server-assigned */
  eventId: string;
  /** e.g. "order.created", "trade.filled" */
  eventType: string;
  /** originating service name */
  sourceService: string;
  /** trace/request correlation */
  correlationId: string;
  occurredAt?:
    | Date
    | undefined;
  /** server write time */
  recordedAt?:
    | Date
    | undefined;
  /** event body (JSON-like) */
  payload?: { [key: string]: any } | undefined;
  metadata: { [key: string]: string };
  /** monotonically increasing per stream_key */
  sequence: number;
  /** partition key (e.g. "order:uuid") */
  streamKey: string;
}

export interface LedgerEvent_MetadataEntry {
  key: string;
  value: string;
}

export interface AppendEventRequest {
  eventType: string;
  sourceService: string;
  correlationId: string;
  streamKey: string;
  payload?: { [key: string]: any } | undefined;
  metadata: { [key: string]: string };
  occurredAt?: Date | undefined;
}

export interface AppendEventRequest_MetadataEntry {
  key: string;
  value: string;
}

export interface AppendEventResponse {
  eventId: string;
  sequence: number;
  recordedAt?: Date | undefined;
}

export interface QueryEventsRequest {
  /** optional filter */
  streamKey: string;
  /** optional filter */
  eventType: string;
  /** optional filter */
  sourceService: string;
  timeRange?: TimeRange | undefined;
  page?:
    | PageRequest
    | undefined;
  /** replay from sequence */
  fromSequence: number;
}

export interface QueryEventsResponse {
  events: LedgerEvent[];
  page?: PageResponse | undefined;
}

export interface StreamEventsRequest {
  streamKey: string;
  eventType: string;
  /** 0 = live tail, >0 = replay then tail */
  fromSequence: number;
}

export interface GetEventRequest {
  eventId: string;
}

function createBaseLedgerEvent(): LedgerEvent {
  return {
    eventId: "",
    eventType: "",
    sourceService: "",
    correlationId: "",
    occurredAt: undefined,
    recordedAt: undefined,
    payload: undefined,
    metadata: {},
    sequence: 0,
    streamKey: "",
  };
}

export const LedgerEvent: MessageFns<LedgerEvent> = {
  encode(message: LedgerEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.eventId !== "") {
      writer.uint32(10).string(message.eventId);
    }
    if (message.eventType !== "") {
      writer.uint32(18).string(message.eventType);
    }
    if (message.sourceService !== "") {
      writer.uint32(26).string(message.sourceService);
    }
    if (message.correlationId !== "") {
      writer.uint32(34).string(message.correlationId);
    }
    if (message.occurredAt !== undefined) {
      Timestamp.encode(toTimestamp(message.occurredAt), writer.uint32(42).fork()).join();
    }
    if (message.recordedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.recordedAt), writer.uint32(50).fork()).join();
    }
    if (message.payload !== undefined) {
      Struct.encode(Struct.wrap(message.payload), writer.uint32(58).fork()).join();
    }
    globalThis.Object.entries(message.metadata).forEach(([key, value]: [string, string]) => {
      LedgerEvent_MetadataEntry.encode({ key: key as any, value }, writer.uint32(66).fork()).join();
    });
    if (message.sequence !== 0) {
      writer.uint32(72).int64(message.sequence);
    }
    if (message.streamKey !== "") {
      writer.uint32(82).string(message.streamKey);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): LedgerEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLedgerEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.eventId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.eventType = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.sourceService = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.correlationId = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.occurredAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.recordedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.payload = Struct.unwrap(Struct.decode(reader, reader.uint32()));
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          const entry8 = LedgerEvent_MetadataEntry.decode(reader, reader.uint32());
          if (entry8.value !== undefined) {
            message.metadata[entry8.key] = entry8.value;
          }
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.sequence = longToNumber(reader.int64());
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.streamKey = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): LedgerEvent {
    return {
      eventId: isSet(object.eventId)
        ? globalThis.String(object.eventId)
        : isSet(object.event_id)
        ? globalThis.String(object.event_id)
        : "",
      eventType: isSet(object.eventType)
        ? globalThis.String(object.eventType)
        : isSet(object.event_type)
        ? globalThis.String(object.event_type)
        : "",
      sourceService: isSet(object.sourceService)
        ? globalThis.String(object.sourceService)
        : isSet(object.source_service)
        ? globalThis.String(object.source_service)
        : "",
      correlationId: isSet(object.correlationId)
        ? globalThis.String(object.correlationId)
        : isSet(object.correlation_id)
        ? globalThis.String(object.correlation_id)
        : "",
      occurredAt: isSet(object.occurredAt)
        ? fromJsonTimestamp(object.occurredAt)
        : isSet(object.occurred_at)
        ? fromJsonTimestamp(object.occurred_at)
        : undefined,
      recordedAt: isSet(object.recordedAt)
        ? fromJsonTimestamp(object.recordedAt)
        : isSet(object.recorded_at)
        ? fromJsonTimestamp(object.recorded_at)
        : undefined,
      payload: isObject(object.payload) ? object.payload : undefined,
      metadata: isObject(object.metadata)
        ? (globalThis.Object.entries(object.metadata) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : {},
      sequence: isSet(object.sequence) ? globalThis.Number(object.sequence) : 0,
      streamKey: isSet(object.streamKey)
        ? globalThis.String(object.streamKey)
        : isSet(object.stream_key)
        ? globalThis.String(object.stream_key)
        : "",
    };
  },

  toJSON(message: LedgerEvent): unknown {
    const obj: any = {};
    if (message.eventId !== "") {
      obj.eventId = message.eventId;
    }
    if (message.eventType !== "") {
      obj.eventType = message.eventType;
    }
    if (message.sourceService !== "") {
      obj.sourceService = message.sourceService;
    }
    if (message.correlationId !== "") {
      obj.correlationId = message.correlationId;
    }
    if (message.occurredAt !== undefined) {
      obj.occurredAt = message.occurredAt.toISOString();
    }
    if (message.recordedAt !== undefined) {
      obj.recordedAt = message.recordedAt.toISOString();
    }
    if (message.payload !== undefined) {
      obj.payload = message.payload;
    }
    if (message.metadata) {
      const entries = globalThis.Object.entries(message.metadata) as [string, string][];
      if (entries.length > 0) {
        obj.metadata = {};
        entries.forEach(([k, v]) => {
          obj.metadata[k] = v;
        });
      }
    }
    if (message.sequence !== 0) {
      obj.sequence = Math.round(message.sequence);
    }
    if (message.streamKey !== "") {
      obj.streamKey = message.streamKey;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<LedgerEvent>, I>>(base?: I): LedgerEvent {
    return LedgerEvent.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<LedgerEvent>, I>>(object: I): LedgerEvent {
    const message = createBaseLedgerEvent();
    message.eventId = object.eventId ?? "";
    message.eventType = object.eventType ?? "";
    message.sourceService = object.sourceService ?? "";
    message.correlationId = object.correlationId ?? "";
    message.occurredAt = object.occurredAt ?? undefined;
    message.recordedAt = object.recordedAt ?? undefined;
    message.payload = object.payload ?? undefined;
    message.metadata = (globalThis.Object.entries(object.metadata ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    message.sequence = object.sequence ?? 0;
    message.streamKey = object.streamKey ?? "";
    return message;
  },
};

function createBaseLedgerEvent_MetadataEntry(): LedgerEvent_MetadataEntry {
  return { key: "", value: "" };
}

export const LedgerEvent_MetadataEntry: MessageFns<LedgerEvent_MetadataEntry> = {
  encode(message: LedgerEvent_MetadataEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): LedgerEvent_MetadataEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLedgerEvent_MetadataEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): LedgerEvent_MetadataEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: LedgerEvent_MetadataEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<LedgerEvent_MetadataEntry>, I>>(base?: I): LedgerEvent_MetadataEntry {
    return LedgerEvent_MetadataEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<LedgerEvent_MetadataEntry>, I>>(object: I): LedgerEvent_MetadataEntry {
    const message = createBaseLedgerEvent_MetadataEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseAppendEventRequest(): AppendEventRequest {
  return {
    eventType: "",
    sourceService: "",
    correlationId: "",
    streamKey: "",
    payload: undefined,
    metadata: {},
    occurredAt: undefined,
  };
}

export const AppendEventRequest: MessageFns<AppendEventRequest> = {
  encode(message: AppendEventRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.eventType !== "") {
      writer.uint32(10).string(message.eventType);
    }
    if (message.sourceService !== "") {
      writer.uint32(18).string(message.sourceService);
    }
    if (message.correlationId !== "") {
      writer.uint32(26).string(message.correlationId);
    }
    if (message.streamKey !== "") {
      writer.uint32(34).string(message.streamKey);
    }
    if (message.payload !== undefined) {
      Struct.encode(Struct.wrap(message.payload), writer.uint32(42).fork()).join();
    }
    globalThis.Object.entries(message.metadata).forEach(([key, value]: [string, string]) => {
      AppendEventRequest_MetadataEntry.encode({ key: key as any, value }, writer.uint32(50).fork()).join();
    });
    if (message.occurredAt !== undefined) {
      Timestamp.encode(toTimestamp(message.occurredAt), writer.uint32(58).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AppendEventRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAppendEventRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.eventType = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.sourceService = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.correlationId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.streamKey = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.payload = Struct.unwrap(Struct.decode(reader, reader.uint32()));
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          const entry6 = AppendEventRequest_MetadataEntry.decode(reader, reader.uint32());
          if (entry6.value !== undefined) {
            message.metadata[entry6.key] = entry6.value;
          }
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.occurredAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AppendEventRequest {
    return {
      eventType: isSet(object.eventType)
        ? globalThis.String(object.eventType)
        : isSet(object.event_type)
        ? globalThis.String(object.event_type)
        : "",
      sourceService: isSet(object.sourceService)
        ? globalThis.String(object.sourceService)
        : isSet(object.source_service)
        ? globalThis.String(object.source_service)
        : "",
      correlationId: isSet(object.correlationId)
        ? globalThis.String(object.correlationId)
        : isSet(object.correlation_id)
        ? globalThis.String(object.correlation_id)
        : "",
      streamKey: isSet(object.streamKey)
        ? globalThis.String(object.streamKey)
        : isSet(object.stream_key)
        ? globalThis.String(object.stream_key)
        : "",
      payload: isObject(object.payload) ? object.payload : undefined,
      metadata: isObject(object.metadata)
        ? (globalThis.Object.entries(object.metadata) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : {},
      occurredAt: isSet(object.occurredAt)
        ? fromJsonTimestamp(object.occurredAt)
        : isSet(object.occurred_at)
        ? fromJsonTimestamp(object.occurred_at)
        : undefined,
    };
  },

  toJSON(message: AppendEventRequest): unknown {
    const obj: any = {};
    if (message.eventType !== "") {
      obj.eventType = message.eventType;
    }
    if (message.sourceService !== "") {
      obj.sourceService = message.sourceService;
    }
    if (message.correlationId !== "") {
      obj.correlationId = message.correlationId;
    }
    if (message.streamKey !== "") {
      obj.streamKey = message.streamKey;
    }
    if (message.payload !== undefined) {
      obj.payload = message.payload;
    }
    if (message.metadata) {
      const entries = globalThis.Object.entries(message.metadata) as [string, string][];
      if (entries.length > 0) {
        obj.metadata = {};
        entries.forEach(([k, v]) => {
          obj.metadata[k] = v;
        });
      }
    }
    if (message.occurredAt !== undefined) {
      obj.occurredAt = message.occurredAt.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AppendEventRequest>, I>>(base?: I): AppendEventRequest {
    return AppendEventRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AppendEventRequest>, I>>(object: I): AppendEventRequest {
    const message = createBaseAppendEventRequest();
    message.eventType = object.eventType ?? "";
    message.sourceService = object.sourceService ?? "";
    message.correlationId = object.correlationId ?? "";
    message.streamKey = object.streamKey ?? "";
    message.payload = object.payload ?? undefined;
    message.metadata = (globalThis.Object.entries(object.metadata ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    message.occurredAt = object.occurredAt ?? undefined;
    return message;
  },
};

function createBaseAppendEventRequest_MetadataEntry(): AppendEventRequest_MetadataEntry {
  return { key: "", value: "" };
}

export const AppendEventRequest_MetadataEntry: MessageFns<AppendEventRequest_MetadataEntry> = {
  encode(message: AppendEventRequest_MetadataEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AppendEventRequest_MetadataEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAppendEventRequest_MetadataEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AppendEventRequest_MetadataEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: AppendEventRequest_MetadataEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AppendEventRequest_MetadataEntry>, I>>(
    base?: I,
  ): AppendEventRequest_MetadataEntry {
    return AppendEventRequest_MetadataEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AppendEventRequest_MetadataEntry>, I>>(
    object: I,
  ): AppendEventRequest_MetadataEntry {
    const message = createBaseAppendEventRequest_MetadataEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseAppendEventResponse(): AppendEventResponse {
  return { eventId: "", sequence: 0, recordedAt: undefined };
}

export const AppendEventResponse: MessageFns<AppendEventResponse> = {
  encode(message: AppendEventResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.eventId !== "") {
      writer.uint32(10).string(message.eventId);
    }
    if (message.sequence !== 0) {
      writer.uint32(16).int64(message.sequence);
    }
    if (message.recordedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.recordedAt), writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AppendEventResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAppendEventResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.eventId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.sequence = longToNumber(reader.int64());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.recordedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AppendEventResponse {
    return {
      eventId: isSet(object.eventId)
        ? globalThis.String(object.eventId)
        : isSet(object.event_id)
        ? globalThis.String(object.event_id)
        : "",
      sequence: isSet(object.sequence) ? globalThis.Number(object.sequence) : 0,
      recordedAt: isSet(object.recordedAt)
        ? fromJsonTimestamp(object.recordedAt)
        : isSet(object.recorded_at)
        ? fromJsonTimestamp(object.recorded_at)
        : undefined,
    };
  },

  toJSON(message: AppendEventResponse): unknown {
    const obj: any = {};
    if (message.eventId !== "") {
      obj.eventId = message.eventId;
    }
    if (message.sequence !== 0) {
      obj.sequence = Math.round(message.sequence);
    }
    if (message.recordedAt !== undefined) {
      obj.recordedAt = message.recordedAt.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AppendEventResponse>, I>>(base?: I): AppendEventResponse {
    return AppendEventResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AppendEventResponse>, I>>(object: I): AppendEventResponse {
    const message = createBaseAppendEventResponse();
    message.eventId = object.eventId ?? "";
    message.sequence = object.sequence ?? 0;
    message.recordedAt = object.recordedAt ?? undefined;
    return message;
  },
};

function createBaseQueryEventsRequest(): QueryEventsRequest {
  return { streamKey: "", eventType: "", sourceService: "", timeRange: undefined, page: undefined, fromSequence: 0 };
}

export const QueryEventsRequest: MessageFns<QueryEventsRequest> = {
  encode(message: QueryEventsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.streamKey !== "") {
      writer.uint32(10).string(message.streamKey);
    }
    if (message.eventType !== "") {
      writer.uint32(18).string(message.eventType);
    }
    if (message.sourceService !== "") {
      writer.uint32(26).string(message.sourceService);
    }
    if (message.timeRange !== undefined) {
      TimeRange.encode(message.timeRange, writer.uint32(34).fork()).join();
    }
    if (message.page !== undefined) {
      PageRequest.encode(message.page, writer.uint32(42).fork()).join();
    }
    if (message.fromSequence !== 0) {
      writer.uint32(48).int64(message.fromSequence);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryEventsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryEventsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.streamKey = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.eventType = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.sourceService = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.timeRange = TimeRange.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.page = PageRequest.decode(reader, reader.uint32());
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.fromSequence = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryEventsRequest {
    return {
      streamKey: isSet(object.streamKey)
        ? globalThis.String(object.streamKey)
        : isSet(object.stream_key)
        ? globalThis.String(object.stream_key)
        : "",
      eventType: isSet(object.eventType)
        ? globalThis.String(object.eventType)
        : isSet(object.event_type)
        ? globalThis.String(object.event_type)
        : "",
      sourceService: isSet(object.sourceService)
        ? globalThis.String(object.sourceService)
        : isSet(object.source_service)
        ? globalThis.String(object.source_service)
        : "",
      timeRange: isSet(object.timeRange)
        ? TimeRange.fromJSON(object.timeRange)
        : isSet(object.time_range)
        ? TimeRange.fromJSON(object.time_range)
        : undefined,
      page: isSet(object.page) ? PageRequest.fromJSON(object.page) : undefined,
      fromSequence: isSet(object.fromSequence)
        ? globalThis.Number(object.fromSequence)
        : isSet(object.from_sequence)
        ? globalThis.Number(object.from_sequence)
        : 0,
    };
  },

  toJSON(message: QueryEventsRequest): unknown {
    const obj: any = {};
    if (message.streamKey !== "") {
      obj.streamKey = message.streamKey;
    }
    if (message.eventType !== "") {
      obj.eventType = message.eventType;
    }
    if (message.sourceService !== "") {
      obj.sourceService = message.sourceService;
    }
    if (message.timeRange !== undefined) {
      obj.timeRange = TimeRange.toJSON(message.timeRange);
    }
    if (message.page !== undefined) {
      obj.page = PageRequest.toJSON(message.page);
    }
    if (message.fromSequence !== 0) {
      obj.fromSequence = Math.round(message.fromSequence);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryEventsRequest>, I>>(base?: I): QueryEventsRequest {
    return QueryEventsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryEventsRequest>, I>>(object: I): QueryEventsRequest {
    const message = createBaseQueryEventsRequest();
    message.streamKey = object.streamKey ?? "";
    message.eventType = object.eventType ?? "";
    message.sourceService = object.sourceService ?? "";
    message.timeRange = (object.timeRange !== undefined && object.timeRange !== null)
      ? TimeRange.fromPartial(object.timeRange)
      : undefined;
    message.page = (object.page !== undefined && object.page !== null)
      ? PageRequest.fromPartial(object.page)
      : undefined;
    message.fromSequence = object.fromSequence ?? 0;
    return message;
  },
};

function createBaseQueryEventsResponse(): QueryEventsResponse {
  return { events: [], page: undefined };
}

export const QueryEventsResponse: MessageFns<QueryEventsResponse> = {
  encode(message: QueryEventsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.events) {
      LedgerEvent.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.page !== undefined) {
      PageResponse.encode(message.page, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryEventsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryEventsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.events.push(LedgerEvent.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.page = PageResponse.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryEventsResponse {
    return {
      events: globalThis.Array.isArray(object?.events) ? object.events.map((e: any) => LedgerEvent.fromJSON(e)) : [],
      page: isSet(object.page) ? PageResponse.fromJSON(object.page) : undefined,
    };
  },

  toJSON(message: QueryEventsResponse): unknown {
    const obj: any = {};
    if (message.events?.length) {
      obj.events = message.events.map((e) => LedgerEvent.toJSON(e));
    }
    if (message.page !== undefined) {
      obj.page = PageResponse.toJSON(message.page);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryEventsResponse>, I>>(base?: I): QueryEventsResponse {
    return QueryEventsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryEventsResponse>, I>>(object: I): QueryEventsResponse {
    const message = createBaseQueryEventsResponse();
    message.events = object.events?.map((e) => LedgerEvent.fromPartial(e)) || [];
    message.page = (object.page !== undefined && object.page !== null)
      ? PageResponse.fromPartial(object.page)
      : undefined;
    return message;
  },
};

function createBaseStreamEventsRequest(): StreamEventsRequest {
  return { streamKey: "", eventType: "", fromSequence: 0 };
}

export const StreamEventsRequest: MessageFns<StreamEventsRequest> = {
  encode(message: StreamEventsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.streamKey !== "") {
      writer.uint32(10).string(message.streamKey);
    }
    if (message.eventType !== "") {
      writer.uint32(18).string(message.eventType);
    }
    if (message.fromSequence !== 0) {
      writer.uint32(24).int64(message.fromSequence);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StreamEventsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStreamEventsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.streamKey = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.eventType = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.fromSequence = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StreamEventsRequest {
    return {
      streamKey: isSet(object.streamKey)
        ? globalThis.String(object.streamKey)
        : isSet(object.stream_key)
        ? globalThis.String(object.stream_key)
        : "",
      eventType: isSet(object.eventType)
        ? globalThis.String(object.eventType)
        : isSet(object.event_type)
        ? globalThis.String(object.event_type)
        : "",
      fromSequence: isSet(object.fromSequence)
        ? globalThis.Number(object.fromSequence)
        : isSet(object.from_sequence)
        ? globalThis.Number(object.from_sequence)
        : 0,
    };
  },

  toJSON(message: StreamEventsRequest): unknown {
    const obj: any = {};
    if (message.streamKey !== "") {
      obj.streamKey = message.streamKey;
    }
    if (message.eventType !== "") {
      obj.eventType = message.eventType;
    }
    if (message.fromSequence !== 0) {
      obj.fromSequence = Math.round(message.fromSequence);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StreamEventsRequest>, I>>(base?: I): StreamEventsRequest {
    return StreamEventsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StreamEventsRequest>, I>>(object: I): StreamEventsRequest {
    const message = createBaseStreamEventsRequest();
    message.streamKey = object.streamKey ?? "";
    message.eventType = object.eventType ?? "";
    message.fromSequence = object.fromSequence ?? 0;
    return message;
  },
};

function createBaseGetEventRequest(): GetEventRequest {
  return { eventId: "" };
}

export const GetEventRequest: MessageFns<GetEventRequest> = {
  encode(message: GetEventRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.eventId !== "") {
      writer.uint32(10).string(message.eventId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetEventRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetEventRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.eventId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetEventRequest {
    return {
      eventId: isSet(object.eventId)
        ? globalThis.String(object.eventId)
        : isSet(object.event_id)
        ? globalThis.String(object.event_id)
        : "",
    };
  },

  toJSON(message: GetEventRequest): unknown {
    const obj: any = {};
    if (message.eventId !== "") {
      obj.eventId = message.eventId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetEventRequest>, I>>(base?: I): GetEventRequest {
    return GetEventRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetEventRequest>, I>>(object: I): GetEventRequest {
    const message = createBaseGetEventRequest();
    message.eventId = object.eventId ?? "";
    return message;
  },
};

/**
 * LedgerService — append-only event store.
 * All services write events here. Events are immutable once written.
 */
export type LedgerServiceService = typeof LedgerServiceService;
export const LedgerServiceService = {
  appendEvent: {
    path: "/xstockstrat.ledger.v1.LedgerService/AppendEvent" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: AppendEventRequest): Buffer => Buffer.from(AppendEventRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): AppendEventRequest => AppendEventRequest.decode(value),
    responseSerialize: (value: AppendEventResponse): Buffer => Buffer.from(AppendEventResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): AppendEventResponse => AppendEventResponse.decode(value),
  },
  queryEvents: {
    path: "/xstockstrat.ledger.v1.LedgerService/QueryEvents" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: QueryEventsRequest): Buffer => Buffer.from(QueryEventsRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): QueryEventsRequest => QueryEventsRequest.decode(value),
    responseSerialize: (value: QueryEventsResponse): Buffer => Buffer.from(QueryEventsResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): QueryEventsResponse => QueryEventsResponse.decode(value),
  },
  streamEvents: {
    path: "/xstockstrat.ledger.v1.LedgerService/StreamEvents" as const,
    requestStream: false as const,
    responseStream: true as const,
    requestSerialize: (value: StreamEventsRequest): Buffer => Buffer.from(StreamEventsRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): StreamEventsRequest => StreamEventsRequest.decode(value),
    responseSerialize: (value: LedgerEvent): Buffer => Buffer.from(LedgerEvent.encode(value).finish()),
    responseDeserialize: (value: Buffer): LedgerEvent => LedgerEvent.decode(value),
  },
  getEvent: {
    path: "/xstockstrat.ledger.v1.LedgerService/GetEvent" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: GetEventRequest): Buffer => Buffer.from(GetEventRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): GetEventRequest => GetEventRequest.decode(value),
    responseSerialize: (value: LedgerEvent): Buffer => Buffer.from(LedgerEvent.encode(value).finish()),
    responseDeserialize: (value: Buffer): LedgerEvent => LedgerEvent.decode(value),
  },
} as const;

export interface LedgerServiceServer extends UntypedServiceImplementation {
  appendEvent: handleUnaryCall<AppendEventRequest, AppendEventResponse>;
  queryEvents: handleUnaryCall<QueryEventsRequest, QueryEventsResponse>;
  streamEvents: handleServerStreamingCall<StreamEventsRequest, LedgerEvent>;
  getEvent: handleUnaryCall<GetEventRequest, LedgerEvent>;
}

export interface LedgerServiceClient extends Client {
  appendEvent(
    request: AppendEventRequest,
    callback: (error: ServiceError | null, response: AppendEventResponse) => void,
  ): ClientUnaryCall;
  appendEvent(
    request: AppendEventRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: AppendEventResponse) => void,
  ): ClientUnaryCall;
  appendEvent(
    request: AppendEventRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: AppendEventResponse) => void,
  ): ClientUnaryCall;
  queryEvents(
    request: QueryEventsRequest,
    callback: (error: ServiceError | null, response: QueryEventsResponse) => void,
  ): ClientUnaryCall;
  queryEvents(
    request: QueryEventsRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: QueryEventsResponse) => void,
  ): ClientUnaryCall;
  queryEvents(
    request: QueryEventsRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: QueryEventsResponse) => void,
  ): ClientUnaryCall;
  streamEvents(request: StreamEventsRequest, options?: Partial<CallOptions>): ClientReadableStream<LedgerEvent>;
  streamEvents(
    request: StreamEventsRequest,
    metadata?: Metadata,
    options?: Partial<CallOptions>,
  ): ClientReadableStream<LedgerEvent>;
  getEvent(
    request: GetEventRequest,
    callback: (error: ServiceError | null, response: LedgerEvent) => void,
  ): ClientUnaryCall;
  getEvent(
    request: GetEventRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: LedgerEvent) => void,
  ): ClientUnaryCall;
  getEvent(
    request: GetEventRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: LedgerEvent) => void,
  ): ClientUnaryCall;
}

export const LedgerServiceClient = makeGenericClientConstructor(
  LedgerServiceService,
  "xstockstrat.ledger.v1.LedgerService",
) as unknown as {
  new (address: string, credentials: ChannelCredentials, options?: Partial<ClientOptions>): LedgerServiceClient;
  service: typeof LedgerServiceService;
  serviceName: string;
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}

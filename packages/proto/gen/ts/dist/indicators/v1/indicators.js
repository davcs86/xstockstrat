"use strict";
// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               unknown
// source: indicators/v1/indicators.proto
Object.defineProperty(exports, "__esModule", { value: true });
exports.IndicatorsServiceClient = exports.IndicatorsServiceService = exports.GetFormulaRequest = exports.RegisterFormulaResponse = exports.RegisterFormulaRequest_InputSchemaEntry = exports.RegisterFormulaRequest = exports.IndicatorMeta = exports.ListIndicatorsResponse = exports.ListIndicatorsRequest = exports.FormulaDefinition_InputSchemaEntry = exports.FormulaDefinition = exports.ExecuteFormulaResponse = exports.ExecuteFormulaRequest_EnvEntry = exports.ExecuteFormulaRequest = exports.IndicatorPoint_ExtraEntry = exports.IndicatorPoint = exports.ComputeIndicatorResponse_ParamsUsedEntry = exports.ComputeIndicatorResponse = exports.ComputeIndicatorRequest_ParamsEntry = exports.ComputeIndicatorRequest = exports.SandboxExitReason = exports.protobufPackage = void 0;
exports.sandboxExitReasonFromJSON = sandboxExitReasonFromJSON;
exports.sandboxExitReasonToJSON = sandboxExitReasonToJSON;
exports.sandboxExitReasonToNumber = sandboxExitReasonToNumber;
/* eslint-disable */
const wire_1 = require("@bufbuild/protobuf/wire");
const grpc_js_1 = require("@grpc/grpc-js");
const common_1 = require("../../common/v1/common");
const struct_1 = require("../../google/protobuf/struct");
const timestamp_1 = require("../../google/protobuf/timestamp");
exports.protobufPackage = "xstockstrat.indicators.v1";
var SandboxExitReason;
(function (SandboxExitReason) {
    SandboxExitReason["SANDBOX_EXIT_REASON_UNSPECIFIED"] = "SANDBOX_EXIT_REASON_UNSPECIFIED";
    SandboxExitReason["SANDBOX_EXIT_REASON_SUCCESS"] = "SANDBOX_EXIT_REASON_SUCCESS";
    SandboxExitReason["SANDBOX_EXIT_REASON_TIMEOUT"] = "SANDBOX_EXIT_REASON_TIMEOUT";
    SandboxExitReason["SANDBOX_EXIT_REASON_MEMORY_EXCEEDED"] = "SANDBOX_EXIT_REASON_MEMORY_EXCEEDED";
    SandboxExitReason["SANDBOX_EXIT_REASON_RUNTIME_ERROR"] = "SANDBOX_EXIT_REASON_RUNTIME_ERROR";
    SandboxExitReason["SANDBOX_EXIT_REASON_IMPORT_BLOCKED"] = "SANDBOX_EXIT_REASON_IMPORT_BLOCKED";
    SandboxExitReason["UNRECOGNIZED"] = "UNRECOGNIZED";
})(SandboxExitReason || (exports.SandboxExitReason = SandboxExitReason = {}));
function sandboxExitReasonFromJSON(object) {
    switch (object) {
        case 0:
        case "SANDBOX_EXIT_REASON_UNSPECIFIED":
            return SandboxExitReason.SANDBOX_EXIT_REASON_UNSPECIFIED;
        case 1:
        case "SANDBOX_EXIT_REASON_SUCCESS":
            return SandboxExitReason.SANDBOX_EXIT_REASON_SUCCESS;
        case 2:
        case "SANDBOX_EXIT_REASON_TIMEOUT":
            return SandboxExitReason.SANDBOX_EXIT_REASON_TIMEOUT;
        case 3:
        case "SANDBOX_EXIT_REASON_MEMORY_EXCEEDED":
            return SandboxExitReason.SANDBOX_EXIT_REASON_MEMORY_EXCEEDED;
        case 4:
        case "SANDBOX_EXIT_REASON_RUNTIME_ERROR":
            return SandboxExitReason.SANDBOX_EXIT_REASON_RUNTIME_ERROR;
        case 5:
        case "SANDBOX_EXIT_REASON_IMPORT_BLOCKED":
            return SandboxExitReason.SANDBOX_EXIT_REASON_IMPORT_BLOCKED;
        case -1:
        case "UNRECOGNIZED":
        default:
            return SandboxExitReason.UNRECOGNIZED;
    }
}
function sandboxExitReasonToJSON(object) {
    switch (object) {
        case SandboxExitReason.SANDBOX_EXIT_REASON_UNSPECIFIED:
            return "SANDBOX_EXIT_REASON_UNSPECIFIED";
        case SandboxExitReason.SANDBOX_EXIT_REASON_SUCCESS:
            return "SANDBOX_EXIT_REASON_SUCCESS";
        case SandboxExitReason.SANDBOX_EXIT_REASON_TIMEOUT:
            return "SANDBOX_EXIT_REASON_TIMEOUT";
        case SandboxExitReason.SANDBOX_EXIT_REASON_MEMORY_EXCEEDED:
            return "SANDBOX_EXIT_REASON_MEMORY_EXCEEDED";
        case SandboxExitReason.SANDBOX_EXIT_REASON_RUNTIME_ERROR:
            return "SANDBOX_EXIT_REASON_RUNTIME_ERROR";
        case SandboxExitReason.SANDBOX_EXIT_REASON_IMPORT_BLOCKED:
            return "SANDBOX_EXIT_REASON_IMPORT_BLOCKED";
        case SandboxExitReason.UNRECOGNIZED:
        default:
            return "UNRECOGNIZED";
    }
}
function sandboxExitReasonToNumber(object) {
    switch (object) {
        case SandboxExitReason.SANDBOX_EXIT_REASON_UNSPECIFIED:
            return 0;
        case SandboxExitReason.SANDBOX_EXIT_REASON_SUCCESS:
            return 1;
        case SandboxExitReason.SANDBOX_EXIT_REASON_TIMEOUT:
            return 2;
        case SandboxExitReason.SANDBOX_EXIT_REASON_MEMORY_EXCEEDED:
            return 3;
        case SandboxExitReason.SANDBOX_EXIT_REASON_RUNTIME_ERROR:
            return 4;
        case SandboxExitReason.SANDBOX_EXIT_REASON_IMPORT_BLOCKED:
            return 5;
        case SandboxExitReason.UNRECOGNIZED:
        default:
            return -1;
    }
}
function createBaseComputeIndicatorRequest() {
    return { indicator: "", values: [], params: {}, range: undefined, symbol: "", timeframe: "" };
}
exports.ComputeIndicatorRequest = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.indicator !== "") {
            writer.uint32(10).string(message.indicator);
        }
        writer.uint32(18).fork();
        for (const v of message.values) {
            writer.double(v);
        }
        writer.join();
        globalThis.Object.entries(message.params).forEach(([key, value]) => {
            exports.ComputeIndicatorRequest_ParamsEntry.encode({ key: key, value }, writer.uint32(26).fork()).join();
        });
        if (message.range !== undefined) {
            common_1.TimeRange.encode(message.range, writer.uint32(34).fork()).join();
        }
        if (message.symbol !== "") {
            writer.uint32(42).string(message.symbol);
        }
        if (message.timeframe !== "") {
            writer.uint32(50).string(message.timeframe);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseComputeIndicatorRequest();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.indicator = reader.string();
                    continue;
                }
                case 2: {
                    if (tag === 17) {
                        message.values.push(reader.double());
                        continue;
                    }
                    if (tag === 18) {
                        const end2 = reader.uint32() + reader.pos;
                        while (reader.pos < end2) {
                            message.values.push(reader.double());
                        }
                        continue;
                    }
                    break;
                }
                case 3: {
                    if (tag !== 26) {
                        break;
                    }
                    const entry3 = exports.ComputeIndicatorRequest_ParamsEntry.decode(reader, reader.uint32());
                    if (entry3.value !== undefined) {
                        message.params[entry3.key] = entry3.value;
                    }
                    continue;
                }
                case 4: {
                    if (tag !== 34) {
                        break;
                    }
                    message.range = common_1.TimeRange.decode(reader, reader.uint32());
                    continue;
                }
                case 5: {
                    if (tag !== 42) {
                        break;
                    }
                    message.symbol = reader.string();
                    continue;
                }
                case 6: {
                    if (tag !== 50) {
                        break;
                    }
                    message.timeframe = reader.string();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            indicator: isSet(object.indicator) ? globalThis.String(object.indicator) : "",
            values: globalThis.Array.isArray(object?.values) ? object.values.map((e) => globalThis.Number(e)) : [],
            params: isObject(object.params)
                ? globalThis.Object.entries(object.params).reduce((acc, [key, value]) => {
                    acc[key] = globalThis.Number(value);
                    return acc;
                }, {})
                : {},
            range: isSet(object.range) ? common_1.TimeRange.fromJSON(object.range) : undefined,
            symbol: isSet(object.symbol) ? globalThis.String(object.symbol) : "",
            timeframe: isSet(object.timeframe) ? globalThis.String(object.timeframe) : "",
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.indicator !== "") {
            obj.indicator = message.indicator;
        }
        if (message.values?.length) {
            obj.values = message.values;
        }
        if (message.params) {
            const entries = globalThis.Object.entries(message.params);
            if (entries.length > 0) {
                obj.params = {};
                entries.forEach(([k, v]) => {
                    obj.params[k] = v;
                });
            }
        }
        if (message.range !== undefined) {
            obj.range = common_1.TimeRange.toJSON(message.range);
        }
        if (message.symbol !== "") {
            obj.symbol = message.symbol;
        }
        if (message.timeframe !== "") {
            obj.timeframe = message.timeframe;
        }
        return obj;
    },
    create(base) {
        return exports.ComputeIndicatorRequest.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseComputeIndicatorRequest();
        message.indicator = object.indicator ?? "";
        message.values = object.values?.map((e) => e) || [];
        message.params = globalThis.Object.entries(object.params ?? {}).reduce((acc, [key, value]) => {
            if (value !== undefined) {
                acc[key] = globalThis.Number(value);
            }
            return acc;
        }, {});
        message.range = (object.range !== undefined && object.range !== null)
            ? common_1.TimeRange.fromPartial(object.range)
            : undefined;
        message.symbol = object.symbol ?? "";
        message.timeframe = object.timeframe ?? "";
        return message;
    },
};
function createBaseComputeIndicatorRequest_ParamsEntry() {
    return { key: "", value: 0 };
}
exports.ComputeIndicatorRequest_ParamsEntry = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.key !== "") {
            writer.uint32(10).string(message.key);
        }
        if (message.value !== 0) {
            writer.uint32(17).double(message.value);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseComputeIndicatorRequest_ParamsEntry();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.key = reader.string();
                    continue;
                }
                case 2: {
                    if (tag !== 17) {
                        break;
                    }
                    message.value = reader.double();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            key: isSet(object.key) ? globalThis.String(object.key) : "",
            value: isSet(object.value) ? globalThis.Number(object.value) : 0,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.key !== "") {
            obj.key = message.key;
        }
        if (message.value !== 0) {
            obj.value = message.value;
        }
        return obj;
    },
    create(base) {
        return exports.ComputeIndicatorRequest_ParamsEntry.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseComputeIndicatorRequest_ParamsEntry();
        message.key = object.key ?? "";
        message.value = object.value ?? 0;
        return message;
    },
};
function createBaseComputeIndicatorResponse() {
    return { result: [], indicator: "", paramsUsed: {} };
}
exports.ComputeIndicatorResponse = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        for (const v of message.result) {
            exports.IndicatorPoint.encode(v, writer.uint32(10).fork()).join();
        }
        if (message.indicator !== "") {
            writer.uint32(18).string(message.indicator);
        }
        globalThis.Object.entries(message.paramsUsed).forEach(([key, value]) => {
            exports.ComputeIndicatorResponse_ParamsUsedEntry.encode({ key: key, value }, writer.uint32(26).fork()).join();
        });
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseComputeIndicatorResponse();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.result.push(exports.IndicatorPoint.decode(reader, reader.uint32()));
                    continue;
                }
                case 2: {
                    if (tag !== 18) {
                        break;
                    }
                    message.indicator = reader.string();
                    continue;
                }
                case 3: {
                    if (tag !== 26) {
                        break;
                    }
                    const entry3 = exports.ComputeIndicatorResponse_ParamsUsedEntry.decode(reader, reader.uint32());
                    if (entry3.value !== undefined) {
                        message.paramsUsed[entry3.key] = entry3.value;
                    }
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            result: globalThis.Array.isArray(object?.result) ? object.result.map((e) => exports.IndicatorPoint.fromJSON(e)) : [],
            indicator: isSet(object.indicator) ? globalThis.String(object.indicator) : "",
            paramsUsed: isObject(object.paramsUsed)
                ? globalThis.Object.entries(object.paramsUsed).reduce((acc, [key, value]) => {
                    acc[key] = globalThis.Number(value);
                    return acc;
                }, {})
                : isObject(object.params_used)
                    ? globalThis.Object.entries(object.params_used).reduce((acc, [key, value]) => {
                        acc[key] = globalThis.Number(value);
                        return acc;
                    }, {})
                    : {},
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.result?.length) {
            obj.result = message.result.map((e) => exports.IndicatorPoint.toJSON(e));
        }
        if (message.indicator !== "") {
            obj.indicator = message.indicator;
        }
        if (message.paramsUsed) {
            const entries = globalThis.Object.entries(message.paramsUsed);
            if (entries.length > 0) {
                obj.paramsUsed = {};
                entries.forEach(([k, v]) => {
                    obj.paramsUsed[k] = v;
                });
            }
        }
        return obj;
    },
    create(base) {
        return exports.ComputeIndicatorResponse.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseComputeIndicatorResponse();
        message.result = object.result?.map((e) => exports.IndicatorPoint.fromPartial(e)) || [];
        message.indicator = object.indicator ?? "";
        message.paramsUsed = globalThis.Object.entries(object.paramsUsed ?? {}).reduce((acc, [key, value]) => {
            if (value !== undefined) {
                acc[key] = globalThis.Number(value);
            }
            return acc;
        }, {});
        return message;
    },
};
function createBaseComputeIndicatorResponse_ParamsUsedEntry() {
    return { key: "", value: 0 };
}
exports.ComputeIndicatorResponse_ParamsUsedEntry = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.key !== "") {
            writer.uint32(10).string(message.key);
        }
        if (message.value !== 0) {
            writer.uint32(17).double(message.value);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseComputeIndicatorResponse_ParamsUsedEntry();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.key = reader.string();
                    continue;
                }
                case 2: {
                    if (tag !== 17) {
                        break;
                    }
                    message.value = reader.double();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            key: isSet(object.key) ? globalThis.String(object.key) : "",
            value: isSet(object.value) ? globalThis.Number(object.value) : 0,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.key !== "") {
            obj.key = message.key;
        }
        if (message.value !== 0) {
            obj.value = message.value;
        }
        return obj;
    },
    create(base) {
        return exports.ComputeIndicatorResponse_ParamsUsedEntry.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseComputeIndicatorResponse_ParamsUsedEntry();
        message.key = object.key ?? "";
        message.value = object.value ?? 0;
        return message;
    },
};
function createBaseIndicatorPoint() {
    return { time: undefined, value: 0, extra: {} };
}
exports.IndicatorPoint = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.time !== undefined) {
            timestamp_1.Timestamp.encode(toTimestamp(message.time), writer.uint32(10).fork()).join();
        }
        if (message.value !== 0) {
            writer.uint32(17).double(message.value);
        }
        globalThis.Object.entries(message.extra).forEach(([key, value]) => {
            exports.IndicatorPoint_ExtraEntry.encode({ key: key, value }, writer.uint32(26).fork()).join();
        });
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseIndicatorPoint();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.time = fromTimestamp(timestamp_1.Timestamp.decode(reader, reader.uint32()));
                    continue;
                }
                case 2: {
                    if (tag !== 17) {
                        break;
                    }
                    message.value = reader.double();
                    continue;
                }
                case 3: {
                    if (tag !== 26) {
                        break;
                    }
                    const entry3 = exports.IndicatorPoint_ExtraEntry.decode(reader, reader.uint32());
                    if (entry3.value !== undefined) {
                        message.extra[entry3.key] = entry3.value;
                    }
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            time: isSet(object.time) ? fromJsonTimestamp(object.time) : undefined,
            value: isSet(object.value) ? globalThis.Number(object.value) : 0,
            extra: isObject(object.extra)
                ? globalThis.Object.entries(object.extra).reduce((acc, [key, value]) => {
                    acc[key] = globalThis.Number(value);
                    return acc;
                }, {})
                : {},
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.time !== undefined) {
            obj.time = message.time.toISOString();
        }
        if (message.value !== 0) {
            obj.value = message.value;
        }
        if (message.extra) {
            const entries = globalThis.Object.entries(message.extra);
            if (entries.length > 0) {
                obj.extra = {};
                entries.forEach(([k, v]) => {
                    obj.extra[k] = v;
                });
            }
        }
        return obj;
    },
    create(base) {
        return exports.IndicatorPoint.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseIndicatorPoint();
        message.time = object.time ?? undefined;
        message.value = object.value ?? 0;
        message.extra = globalThis.Object.entries(object.extra ?? {}).reduce((acc, [key, value]) => {
            if (value !== undefined) {
                acc[key] = globalThis.Number(value);
            }
            return acc;
        }, {});
        return message;
    },
};
function createBaseIndicatorPoint_ExtraEntry() {
    return { key: "", value: 0 };
}
exports.IndicatorPoint_ExtraEntry = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.key !== "") {
            writer.uint32(10).string(message.key);
        }
        if (message.value !== 0) {
            writer.uint32(17).double(message.value);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseIndicatorPoint_ExtraEntry();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.key = reader.string();
                    continue;
                }
                case 2: {
                    if (tag !== 17) {
                        break;
                    }
                    message.value = reader.double();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            key: isSet(object.key) ? globalThis.String(object.key) : "",
            value: isSet(object.value) ? globalThis.Number(object.value) : 0,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.key !== "") {
            obj.key = message.key;
        }
        if (message.value !== 0) {
            obj.value = message.value;
        }
        return obj;
    },
    create(base) {
        return exports.IndicatorPoint_ExtraEntry.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseIndicatorPoint_ExtraEntry();
        message.key = object.key ?? "";
        message.value = object.value ?? 0;
        return message;
    },
};
function createBaseExecuteFormulaRequest() {
    return {
        formulaId: "",
        formulaSource: "",
        inputData: undefined,
        env: {},
        timeoutMsOverride: 0,
        memoryBytesOverride: 0,
    };
}
exports.ExecuteFormulaRequest = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.formulaId !== "") {
            writer.uint32(10).string(message.formulaId);
        }
        if (message.formulaSource !== "") {
            writer.uint32(18).string(message.formulaSource);
        }
        if (message.inputData !== undefined) {
            struct_1.Struct.encode(struct_1.Struct.wrap(message.inputData), writer.uint32(26).fork()).join();
        }
        globalThis.Object.entries(message.env).forEach(([key, value]) => {
            exports.ExecuteFormulaRequest_EnvEntry.encode({ key: key, value }, writer.uint32(34).fork()).join();
        });
        if (message.timeoutMsOverride !== 0) {
            writer.uint32(40).int32(message.timeoutMsOverride);
        }
        if (message.memoryBytesOverride !== 0) {
            writer.uint32(48).int64(message.memoryBytesOverride);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseExecuteFormulaRequest();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.formulaId = reader.string();
                    continue;
                }
                case 2: {
                    if (tag !== 18) {
                        break;
                    }
                    message.formulaSource = reader.string();
                    continue;
                }
                case 3: {
                    if (tag !== 26) {
                        break;
                    }
                    message.inputData = struct_1.Struct.unwrap(struct_1.Struct.decode(reader, reader.uint32()));
                    continue;
                }
                case 4: {
                    if (tag !== 34) {
                        break;
                    }
                    const entry4 = exports.ExecuteFormulaRequest_EnvEntry.decode(reader, reader.uint32());
                    if (entry4.value !== undefined) {
                        message.env[entry4.key] = entry4.value;
                    }
                    continue;
                }
                case 5: {
                    if (tag !== 40) {
                        break;
                    }
                    message.timeoutMsOverride = reader.int32();
                    continue;
                }
                case 6: {
                    if (tag !== 48) {
                        break;
                    }
                    message.memoryBytesOverride = longToNumber(reader.int64());
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            formulaId: isSet(object.formulaId)
                ? globalThis.String(object.formulaId)
                : isSet(object.formula_id)
                    ? globalThis.String(object.formula_id)
                    : "",
            formulaSource: isSet(object.formulaSource)
                ? globalThis.String(object.formulaSource)
                : isSet(object.formula_source)
                    ? globalThis.String(object.formula_source)
                    : "",
            inputData: isObject(object.inputData)
                ? object.inputData
                : isObject(object.input_data)
                    ? object.input_data
                    : undefined,
            env: isObject(object.env)
                ? globalThis.Object.entries(object.env).reduce((acc, [key, value]) => {
                    acc[key] = globalThis.String(value);
                    return acc;
                }, {})
                : {},
            timeoutMsOverride: isSet(object.timeoutMsOverride)
                ? globalThis.Number(object.timeoutMsOverride)
                : isSet(object.timeout_ms_override)
                    ? globalThis.Number(object.timeout_ms_override)
                    : 0,
            memoryBytesOverride: isSet(object.memoryBytesOverride)
                ? globalThis.Number(object.memoryBytesOverride)
                : isSet(object.memory_bytes_override)
                    ? globalThis.Number(object.memory_bytes_override)
                    : 0,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.formulaId !== "") {
            obj.formulaId = message.formulaId;
        }
        if (message.formulaSource !== "") {
            obj.formulaSource = message.formulaSource;
        }
        if (message.inputData !== undefined) {
            obj.inputData = message.inputData;
        }
        if (message.env) {
            const entries = globalThis.Object.entries(message.env);
            if (entries.length > 0) {
                obj.env = {};
                entries.forEach(([k, v]) => {
                    obj.env[k] = v;
                });
            }
        }
        if (message.timeoutMsOverride !== 0) {
            obj.timeoutMsOverride = Math.round(message.timeoutMsOverride);
        }
        if (message.memoryBytesOverride !== 0) {
            obj.memoryBytesOverride = Math.round(message.memoryBytesOverride);
        }
        return obj;
    },
    create(base) {
        return exports.ExecuteFormulaRequest.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseExecuteFormulaRequest();
        message.formulaId = object.formulaId ?? "";
        message.formulaSource = object.formulaSource ?? "";
        message.inputData = object.inputData ?? undefined;
        message.env = globalThis.Object.entries(object.env ?? {}).reduce((acc, [key, value]) => {
            if (value !== undefined) {
                acc[key] = globalThis.String(value);
            }
            return acc;
        }, {});
        message.timeoutMsOverride = object.timeoutMsOverride ?? 0;
        message.memoryBytesOverride = object.memoryBytesOverride ?? 0;
        return message;
    },
};
function createBaseExecuteFormulaRequest_EnvEntry() {
    return { key: "", value: "" };
}
exports.ExecuteFormulaRequest_EnvEntry = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.key !== "") {
            writer.uint32(10).string(message.key);
        }
        if (message.value !== "") {
            writer.uint32(18).string(message.value);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseExecuteFormulaRequest_EnvEntry();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.key = reader.string();
                    continue;
                }
                case 2: {
                    if (tag !== 18) {
                        break;
                    }
                    message.value = reader.string();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            key: isSet(object.key) ? globalThis.String(object.key) : "",
            value: isSet(object.value) ? globalThis.String(object.value) : "",
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.key !== "") {
            obj.key = message.key;
        }
        if (message.value !== "") {
            obj.value = message.value;
        }
        return obj;
    },
    create(base) {
        return exports.ExecuteFormulaRequest_EnvEntry.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseExecuteFormulaRequest_EnvEntry();
        message.key = object.key ?? "";
        message.value = object.value ?? "";
        return message;
    },
};
function createBaseExecuteFormulaResponse() {
    return {
        success: false,
        output: undefined,
        stdout: "",
        stderr: "",
        executionMs: 0,
        memoryUsedBytes: 0,
        error: "",
        exitReason: SandboxExitReason.SANDBOX_EXIT_REASON_UNSPECIFIED,
    };
}
exports.ExecuteFormulaResponse = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.success !== false) {
            writer.uint32(8).bool(message.success);
        }
        if (message.output !== undefined) {
            struct_1.Struct.encode(struct_1.Struct.wrap(message.output), writer.uint32(18).fork()).join();
        }
        if (message.stdout !== "") {
            writer.uint32(26).string(message.stdout);
        }
        if (message.stderr !== "") {
            writer.uint32(34).string(message.stderr);
        }
        if (message.executionMs !== 0) {
            writer.uint32(40).int64(message.executionMs);
        }
        if (message.memoryUsedBytes !== 0) {
            writer.uint32(48).int64(message.memoryUsedBytes);
        }
        if (message.error !== "") {
            writer.uint32(58).string(message.error);
        }
        if (message.exitReason !== SandboxExitReason.SANDBOX_EXIT_REASON_UNSPECIFIED) {
            writer.uint32(64).int32(sandboxExitReasonToNumber(message.exitReason));
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseExecuteFormulaResponse();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 8) {
                        break;
                    }
                    message.success = reader.bool();
                    continue;
                }
                case 2: {
                    if (tag !== 18) {
                        break;
                    }
                    message.output = struct_1.Struct.unwrap(struct_1.Struct.decode(reader, reader.uint32()));
                    continue;
                }
                case 3: {
                    if (tag !== 26) {
                        break;
                    }
                    message.stdout = reader.string();
                    continue;
                }
                case 4: {
                    if (tag !== 34) {
                        break;
                    }
                    message.stderr = reader.string();
                    continue;
                }
                case 5: {
                    if (tag !== 40) {
                        break;
                    }
                    message.executionMs = longToNumber(reader.int64());
                    continue;
                }
                case 6: {
                    if (tag !== 48) {
                        break;
                    }
                    message.memoryUsedBytes = longToNumber(reader.int64());
                    continue;
                }
                case 7: {
                    if (tag !== 58) {
                        break;
                    }
                    message.error = reader.string();
                    continue;
                }
                case 8: {
                    if (tag !== 64) {
                        break;
                    }
                    message.exitReason = sandboxExitReasonFromJSON(reader.int32());
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            success: isSet(object.success) ? globalThis.Boolean(object.success) : false,
            output: isObject(object.output) ? object.output : undefined,
            stdout: isSet(object.stdout) ? globalThis.String(object.stdout) : "",
            stderr: isSet(object.stderr) ? globalThis.String(object.stderr) : "",
            executionMs: isSet(object.executionMs)
                ? globalThis.Number(object.executionMs)
                : isSet(object.execution_ms)
                    ? globalThis.Number(object.execution_ms)
                    : 0,
            memoryUsedBytes: isSet(object.memoryUsedBytes)
                ? globalThis.Number(object.memoryUsedBytes)
                : isSet(object.memory_used_bytes)
                    ? globalThis.Number(object.memory_used_bytes)
                    : 0,
            error: isSet(object.error) ? globalThis.String(object.error) : "",
            exitReason: isSet(object.exitReason)
                ? sandboxExitReasonFromJSON(object.exitReason)
                : isSet(object.exit_reason)
                    ? sandboxExitReasonFromJSON(object.exit_reason)
                    : SandboxExitReason.SANDBOX_EXIT_REASON_UNSPECIFIED,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.success !== false) {
            obj.success = message.success;
        }
        if (message.output !== undefined) {
            obj.output = message.output;
        }
        if (message.stdout !== "") {
            obj.stdout = message.stdout;
        }
        if (message.stderr !== "") {
            obj.stderr = message.stderr;
        }
        if (message.executionMs !== 0) {
            obj.executionMs = Math.round(message.executionMs);
        }
        if (message.memoryUsedBytes !== 0) {
            obj.memoryUsedBytes = Math.round(message.memoryUsedBytes);
        }
        if (message.error !== "") {
            obj.error = message.error;
        }
        if (message.exitReason !== SandboxExitReason.SANDBOX_EXIT_REASON_UNSPECIFIED) {
            obj.exitReason = sandboxExitReasonToJSON(message.exitReason);
        }
        return obj;
    },
    create(base) {
        return exports.ExecuteFormulaResponse.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseExecuteFormulaResponse();
        message.success = object.success ?? false;
        message.output = object.output ?? undefined;
        message.stdout = object.stdout ?? "";
        message.stderr = object.stderr ?? "";
        message.executionMs = object.executionMs ?? 0;
        message.memoryUsedBytes = object.memoryUsedBytes ?? 0;
        message.error = object.error ?? "";
        message.exitReason = object.exitReason ?? SandboxExitReason.SANDBOX_EXIT_REASON_UNSPECIFIED;
        return message;
    },
};
function createBaseFormulaDefinition() {
    return {
        formulaId: "",
        name: "",
        description: "",
        source: "",
        author: "",
        createdAt: undefined,
        updatedAt: undefined,
        isPublic: false,
        inputSchema: {},
    };
}
exports.FormulaDefinition = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.formulaId !== "") {
            writer.uint32(10).string(message.formulaId);
        }
        if (message.name !== "") {
            writer.uint32(18).string(message.name);
        }
        if (message.description !== "") {
            writer.uint32(26).string(message.description);
        }
        if (message.source !== "") {
            writer.uint32(34).string(message.source);
        }
        if (message.author !== "") {
            writer.uint32(42).string(message.author);
        }
        if (message.createdAt !== undefined) {
            timestamp_1.Timestamp.encode(toTimestamp(message.createdAt), writer.uint32(50).fork()).join();
        }
        if (message.updatedAt !== undefined) {
            timestamp_1.Timestamp.encode(toTimestamp(message.updatedAt), writer.uint32(58).fork()).join();
        }
        if (message.isPublic !== false) {
            writer.uint32(64).bool(message.isPublic);
        }
        globalThis.Object.entries(message.inputSchema).forEach(([key, value]) => {
            exports.FormulaDefinition_InputSchemaEntry.encode({ key: key, value }, writer.uint32(74).fork()).join();
        });
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseFormulaDefinition();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.formulaId = reader.string();
                    continue;
                }
                case 2: {
                    if (tag !== 18) {
                        break;
                    }
                    message.name = reader.string();
                    continue;
                }
                case 3: {
                    if (tag !== 26) {
                        break;
                    }
                    message.description = reader.string();
                    continue;
                }
                case 4: {
                    if (tag !== 34) {
                        break;
                    }
                    message.source = reader.string();
                    continue;
                }
                case 5: {
                    if (tag !== 42) {
                        break;
                    }
                    message.author = reader.string();
                    continue;
                }
                case 6: {
                    if (tag !== 50) {
                        break;
                    }
                    message.createdAt = fromTimestamp(timestamp_1.Timestamp.decode(reader, reader.uint32()));
                    continue;
                }
                case 7: {
                    if (tag !== 58) {
                        break;
                    }
                    message.updatedAt = fromTimestamp(timestamp_1.Timestamp.decode(reader, reader.uint32()));
                    continue;
                }
                case 8: {
                    if (tag !== 64) {
                        break;
                    }
                    message.isPublic = reader.bool();
                    continue;
                }
                case 9: {
                    if (tag !== 74) {
                        break;
                    }
                    const entry9 = exports.FormulaDefinition_InputSchemaEntry.decode(reader, reader.uint32());
                    if (entry9.value !== undefined) {
                        message.inputSchema[entry9.key] = entry9.value;
                    }
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            formulaId: isSet(object.formulaId)
                ? globalThis.String(object.formulaId)
                : isSet(object.formula_id)
                    ? globalThis.String(object.formula_id)
                    : "",
            name: isSet(object.name) ? globalThis.String(object.name) : "",
            description: isSet(object.description) ? globalThis.String(object.description) : "",
            source: isSet(object.source) ? globalThis.String(object.source) : "",
            author: isSet(object.author) ? globalThis.String(object.author) : "",
            createdAt: isSet(object.createdAt)
                ? fromJsonTimestamp(object.createdAt)
                : isSet(object.created_at)
                    ? fromJsonTimestamp(object.created_at)
                    : undefined,
            updatedAt: isSet(object.updatedAt)
                ? fromJsonTimestamp(object.updatedAt)
                : isSet(object.updated_at)
                    ? fromJsonTimestamp(object.updated_at)
                    : undefined,
            isPublic: isSet(object.isPublic)
                ? globalThis.Boolean(object.isPublic)
                : isSet(object.is_public)
                    ? globalThis.Boolean(object.is_public)
                    : false,
            inputSchema: isObject(object.inputSchema)
                ? globalThis.Object.entries(object.inputSchema).reduce((acc, [key, value]) => {
                    acc[key] = globalThis.String(value);
                    return acc;
                }, {})
                : isObject(object.input_schema)
                    ? globalThis.Object.entries(object.input_schema).reduce((acc, [key, value]) => {
                        acc[key] = globalThis.String(value);
                        return acc;
                    }, {})
                    : {},
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.formulaId !== "") {
            obj.formulaId = message.formulaId;
        }
        if (message.name !== "") {
            obj.name = message.name;
        }
        if (message.description !== "") {
            obj.description = message.description;
        }
        if (message.source !== "") {
            obj.source = message.source;
        }
        if (message.author !== "") {
            obj.author = message.author;
        }
        if (message.createdAt !== undefined) {
            obj.createdAt = message.createdAt.toISOString();
        }
        if (message.updatedAt !== undefined) {
            obj.updatedAt = message.updatedAt.toISOString();
        }
        if (message.isPublic !== false) {
            obj.isPublic = message.isPublic;
        }
        if (message.inputSchema) {
            const entries = globalThis.Object.entries(message.inputSchema);
            if (entries.length > 0) {
                obj.inputSchema = {};
                entries.forEach(([k, v]) => {
                    obj.inputSchema[k] = v;
                });
            }
        }
        return obj;
    },
    create(base) {
        return exports.FormulaDefinition.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseFormulaDefinition();
        message.formulaId = object.formulaId ?? "";
        message.name = object.name ?? "";
        message.description = object.description ?? "";
        message.source = object.source ?? "";
        message.author = object.author ?? "";
        message.createdAt = object.createdAt ?? undefined;
        message.updatedAt = object.updatedAt ?? undefined;
        message.isPublic = object.isPublic ?? false;
        message.inputSchema = globalThis.Object.entries(object.inputSchema ?? {}).reduce((acc, [key, value]) => {
            if (value !== undefined) {
                acc[key] = globalThis.String(value);
            }
            return acc;
        }, {});
        return message;
    },
};
function createBaseFormulaDefinition_InputSchemaEntry() {
    return { key: "", value: "" };
}
exports.FormulaDefinition_InputSchemaEntry = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.key !== "") {
            writer.uint32(10).string(message.key);
        }
        if (message.value !== "") {
            writer.uint32(18).string(message.value);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseFormulaDefinition_InputSchemaEntry();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.key = reader.string();
                    continue;
                }
                case 2: {
                    if (tag !== 18) {
                        break;
                    }
                    message.value = reader.string();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            key: isSet(object.key) ? globalThis.String(object.key) : "",
            value: isSet(object.value) ? globalThis.String(object.value) : "",
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.key !== "") {
            obj.key = message.key;
        }
        if (message.value !== "") {
            obj.value = message.value;
        }
        return obj;
    },
    create(base) {
        return exports.FormulaDefinition_InputSchemaEntry.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseFormulaDefinition_InputSchemaEntry();
        message.key = object.key ?? "";
        message.value = object.value ?? "";
        return message;
    },
};
function createBaseListIndicatorsRequest() {
    return {};
}
exports.ListIndicatorsRequest = {
    encode(_, writer = new wire_1.BinaryWriter()) {
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseListIndicatorsRequest();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(_) {
        return {};
    },
    toJSON(_) {
        const obj = {};
        return obj;
    },
    create(base) {
        return exports.ListIndicatorsRequest.fromPartial(base ?? {});
    },
    fromPartial(_) {
        const message = createBaseListIndicatorsRequest();
        return message;
    },
};
function createBaseListIndicatorsResponse() {
    return { indicators: [] };
}
exports.ListIndicatorsResponse = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        for (const v of message.indicators) {
            exports.IndicatorMeta.encode(v, writer.uint32(10).fork()).join();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseListIndicatorsResponse();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.indicators.push(exports.IndicatorMeta.decode(reader, reader.uint32()));
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            indicators: globalThis.Array.isArray(object?.indicators)
                ? object.indicators.map((e) => exports.IndicatorMeta.fromJSON(e))
                : [],
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.indicators?.length) {
            obj.indicators = message.indicators.map((e) => exports.IndicatorMeta.toJSON(e));
        }
        return obj;
    },
    create(base) {
        return exports.ListIndicatorsResponse.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseListIndicatorsResponse();
        message.indicators = object.indicators?.map((e) => exports.IndicatorMeta.fromPartial(e)) || [];
        return message;
    },
};
function createBaseIndicatorMeta() {
    return { name: "", description: "", requiredParams: [], optionalParams: [] };
}
exports.IndicatorMeta = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.name !== "") {
            writer.uint32(10).string(message.name);
        }
        if (message.description !== "") {
            writer.uint32(18).string(message.description);
        }
        for (const v of message.requiredParams) {
            writer.uint32(26).string(v);
        }
        for (const v of message.optionalParams) {
            writer.uint32(34).string(v);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseIndicatorMeta();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.name = reader.string();
                    continue;
                }
                case 2: {
                    if (tag !== 18) {
                        break;
                    }
                    message.description = reader.string();
                    continue;
                }
                case 3: {
                    if (tag !== 26) {
                        break;
                    }
                    message.requiredParams.push(reader.string());
                    continue;
                }
                case 4: {
                    if (tag !== 34) {
                        break;
                    }
                    message.optionalParams.push(reader.string());
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            name: isSet(object.name) ? globalThis.String(object.name) : "",
            description: isSet(object.description) ? globalThis.String(object.description) : "",
            requiredParams: globalThis.Array.isArray(object?.requiredParams)
                ? object.requiredParams.map((e) => globalThis.String(e))
                : globalThis.Array.isArray(object?.required_params)
                    ? object.required_params.map((e) => globalThis.String(e))
                    : [],
            optionalParams: globalThis.Array.isArray(object?.optionalParams)
                ? object.optionalParams.map((e) => globalThis.String(e))
                : globalThis.Array.isArray(object?.optional_params)
                    ? object.optional_params.map((e) => globalThis.String(e))
                    : [],
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.name !== "") {
            obj.name = message.name;
        }
        if (message.description !== "") {
            obj.description = message.description;
        }
        if (message.requiredParams?.length) {
            obj.requiredParams = message.requiredParams;
        }
        if (message.optionalParams?.length) {
            obj.optionalParams = message.optionalParams;
        }
        return obj;
    },
    create(base) {
        return exports.IndicatorMeta.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseIndicatorMeta();
        message.name = object.name ?? "";
        message.description = object.description ?? "";
        message.requiredParams = object.requiredParams?.map((e) => e) || [];
        message.optionalParams = object.optionalParams?.map((e) => e) || [];
        return message;
    },
};
function createBaseRegisterFormulaRequest() {
    return { name: "", description: "", source: "", isPublic: false, inputSchema: {} };
}
exports.RegisterFormulaRequest = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.name !== "") {
            writer.uint32(10).string(message.name);
        }
        if (message.description !== "") {
            writer.uint32(18).string(message.description);
        }
        if (message.source !== "") {
            writer.uint32(26).string(message.source);
        }
        if (message.isPublic !== false) {
            writer.uint32(32).bool(message.isPublic);
        }
        globalThis.Object.entries(message.inputSchema).forEach(([key, value]) => {
            exports.RegisterFormulaRequest_InputSchemaEntry.encode({ key: key, value }, writer.uint32(42).fork()).join();
        });
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseRegisterFormulaRequest();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.name = reader.string();
                    continue;
                }
                case 2: {
                    if (tag !== 18) {
                        break;
                    }
                    message.description = reader.string();
                    continue;
                }
                case 3: {
                    if (tag !== 26) {
                        break;
                    }
                    message.source = reader.string();
                    continue;
                }
                case 4: {
                    if (tag !== 32) {
                        break;
                    }
                    message.isPublic = reader.bool();
                    continue;
                }
                case 5: {
                    if (tag !== 42) {
                        break;
                    }
                    const entry5 = exports.RegisterFormulaRequest_InputSchemaEntry.decode(reader, reader.uint32());
                    if (entry5.value !== undefined) {
                        message.inputSchema[entry5.key] = entry5.value;
                    }
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            name: isSet(object.name) ? globalThis.String(object.name) : "",
            description: isSet(object.description) ? globalThis.String(object.description) : "",
            source: isSet(object.source) ? globalThis.String(object.source) : "",
            isPublic: isSet(object.isPublic)
                ? globalThis.Boolean(object.isPublic)
                : isSet(object.is_public)
                    ? globalThis.Boolean(object.is_public)
                    : false,
            inputSchema: isObject(object.inputSchema)
                ? globalThis.Object.entries(object.inputSchema).reduce((acc, [key, value]) => {
                    acc[key] = globalThis.String(value);
                    return acc;
                }, {})
                : isObject(object.input_schema)
                    ? globalThis.Object.entries(object.input_schema).reduce((acc, [key, value]) => {
                        acc[key] = globalThis.String(value);
                        return acc;
                    }, {})
                    : {},
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.name !== "") {
            obj.name = message.name;
        }
        if (message.description !== "") {
            obj.description = message.description;
        }
        if (message.source !== "") {
            obj.source = message.source;
        }
        if (message.isPublic !== false) {
            obj.isPublic = message.isPublic;
        }
        if (message.inputSchema) {
            const entries = globalThis.Object.entries(message.inputSchema);
            if (entries.length > 0) {
                obj.inputSchema = {};
                entries.forEach(([k, v]) => {
                    obj.inputSchema[k] = v;
                });
            }
        }
        return obj;
    },
    create(base) {
        return exports.RegisterFormulaRequest.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseRegisterFormulaRequest();
        message.name = object.name ?? "";
        message.description = object.description ?? "";
        message.source = object.source ?? "";
        message.isPublic = object.isPublic ?? false;
        message.inputSchema = globalThis.Object.entries(object.inputSchema ?? {}).reduce((acc, [key, value]) => {
            if (value !== undefined) {
                acc[key] = globalThis.String(value);
            }
            return acc;
        }, {});
        return message;
    },
};
function createBaseRegisterFormulaRequest_InputSchemaEntry() {
    return { key: "", value: "" };
}
exports.RegisterFormulaRequest_InputSchemaEntry = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.key !== "") {
            writer.uint32(10).string(message.key);
        }
        if (message.value !== "") {
            writer.uint32(18).string(message.value);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseRegisterFormulaRequest_InputSchemaEntry();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.key = reader.string();
                    continue;
                }
                case 2: {
                    if (tag !== 18) {
                        break;
                    }
                    message.value = reader.string();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            key: isSet(object.key) ? globalThis.String(object.key) : "",
            value: isSet(object.value) ? globalThis.String(object.value) : "",
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.key !== "") {
            obj.key = message.key;
        }
        if (message.value !== "") {
            obj.value = message.value;
        }
        return obj;
    },
    create(base) {
        return exports.RegisterFormulaRequest_InputSchemaEntry.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseRegisterFormulaRequest_InputSchemaEntry();
        message.key = object.key ?? "";
        message.value = object.value ?? "";
        return message;
    },
};
function createBaseRegisterFormulaResponse() {
    return { formulaId: "" };
}
exports.RegisterFormulaResponse = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.formulaId !== "") {
            writer.uint32(10).string(message.formulaId);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseRegisterFormulaResponse();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.formulaId = reader.string();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            formulaId: isSet(object.formulaId)
                ? globalThis.String(object.formulaId)
                : isSet(object.formula_id)
                    ? globalThis.String(object.formula_id)
                    : "",
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.formulaId !== "") {
            obj.formulaId = message.formulaId;
        }
        return obj;
    },
    create(base) {
        return exports.RegisterFormulaResponse.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseRegisterFormulaResponse();
        message.formulaId = object.formulaId ?? "";
        return message;
    },
};
function createBaseGetFormulaRequest() {
    return { formulaId: "" };
}
exports.GetFormulaRequest = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.formulaId !== "") {
            writer.uint32(10).string(message.formulaId);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseGetFormulaRequest();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.formulaId = reader.string();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            formulaId: isSet(object.formulaId)
                ? globalThis.String(object.formulaId)
                : isSet(object.formula_id)
                    ? globalThis.String(object.formula_id)
                    : "",
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.formulaId !== "") {
            obj.formulaId = message.formulaId;
        }
        return obj;
    },
    create(base) {
        return exports.GetFormulaRequest.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseGetFormulaRequest();
        message.formulaId = object.formulaId ?? "";
        return message;
    },
};
exports.IndicatorsServiceService = {
    /** Compute a built-in indicator (e.g. SMA, EMA, RSI, MACD, BB) */
    computeIndicator: {
        path: "/xstockstrat.indicators.v1.IndicatorsService/ComputeIndicator",
        requestStream: false,
        responseStream: false,
        requestSerialize: (value) => Buffer.from(exports.ComputeIndicatorRequest.encode(value).finish()),
        requestDeserialize: (value) => exports.ComputeIndicatorRequest.decode(value),
        responseSerialize: (value) => Buffer.from(exports.ComputeIndicatorResponse.encode(value).finish()),
        responseDeserialize: (value) => exports.ComputeIndicatorResponse.decode(value),
    },
    /**
     * Execute a custom Python formula in a sandboxed environment
     * Timeout and memory cap sourced from config: indicators.sandbox.*
     */
    executeFormula: {
        path: "/xstockstrat.indicators.v1.IndicatorsService/ExecuteFormula",
        requestStream: false,
        responseStream: false,
        requestSerialize: (value) => Buffer.from(exports.ExecuteFormulaRequest.encode(value).finish()),
        requestDeserialize: (value) => exports.ExecuteFormulaRequest.decode(value),
        responseSerialize: (value) => Buffer.from(exports.ExecuteFormulaResponse.encode(value).finish()),
        responseDeserialize: (value) => exports.ExecuteFormulaResponse.decode(value),
    },
    /** List available built-in indicators */
    listIndicators: {
        path: "/xstockstrat.indicators.v1.IndicatorsService/ListIndicators",
        requestStream: false,
        responseStream: false,
        requestSerialize: (value) => Buffer.from(exports.ListIndicatorsRequest.encode(value).finish()),
        requestDeserialize: (value) => exports.ListIndicatorsRequest.decode(value),
        responseSerialize: (value) => Buffer.from(exports.ListIndicatorsResponse.encode(value).finish()),
        responseDeserialize: (value) => exports.ListIndicatorsResponse.decode(value),
    },
    /** Register a custom formula definition */
    registerFormula: {
        path: "/xstockstrat.indicators.v1.IndicatorsService/RegisterFormula",
        requestStream: false,
        responseStream: false,
        requestSerialize: (value) => Buffer.from(exports.RegisterFormulaRequest.encode(value).finish()),
        requestDeserialize: (value) => exports.RegisterFormulaRequest.decode(value),
        responseSerialize: (value) => Buffer.from(exports.RegisterFormulaResponse.encode(value).finish()),
        responseDeserialize: (value) => exports.RegisterFormulaResponse.decode(value),
    },
    /** Get a registered formula */
    getFormula: {
        path: "/xstockstrat.indicators.v1.IndicatorsService/GetFormula",
        requestStream: false,
        responseStream: false,
        requestSerialize: (value) => Buffer.from(exports.GetFormulaRequest.encode(value).finish()),
        requestDeserialize: (value) => exports.GetFormulaRequest.decode(value),
        responseSerialize: (value) => Buffer.from(exports.FormulaDefinition.encode(value).finish()),
        responseDeserialize: (value) => exports.FormulaDefinition.decode(value),
    },
};
exports.IndicatorsServiceClient = (0, grpc_js_1.makeGenericClientConstructor)(exports.IndicatorsServiceService, "xstockstrat.indicators.v1.IndicatorsService");
function toTimestamp(date) {
    const seconds = Math.trunc(date.getTime() / 1000);
    const nanos = (date.getTime() % 1000) * 1000000;
    return { seconds, nanos };
}
function fromTimestamp(t) {
    let millis = (t.seconds || 0) * 1000;
    millis += (t.nanos || 0) / 1000000;
    return new globalThis.Date(millis);
}
function fromJsonTimestamp(o) {
    if (o instanceof globalThis.Date) {
        return o;
    }
    else if (typeof o === "string") {
        return new globalThis.Date(o);
    }
    else {
        return fromTimestamp(timestamp_1.Timestamp.fromJSON(o));
    }
}
function longToNumber(int64) {
    const num = globalThis.Number(int64.toString());
    if (num > globalThis.Number.MAX_SAFE_INTEGER) {
        throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
    }
    if (num < globalThis.Number.MIN_SAFE_INTEGER) {
        throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
    }
    return num;
}
function isObject(value) {
    return typeof value === "object" && value !== null;
}
function isSet(value) {
    return value !== null && value !== undefined;
}

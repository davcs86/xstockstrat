failure-threshold: warning
ignore:
  - DL3007  # using :latest tag — intentional for distroless base images
  - DL3008  # apt-get without version pins — acceptable in build/codegen containers
  - DL3059  # multiple RUN commands — too noisy in multi-stage builds

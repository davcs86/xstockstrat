<!-- Generated by constitution-forge (https://github.com/davcs86/agent-plugins) from static code + git-history analysis. AI-derived: every rule cites a path:line or commit, but the set has NOT been human-verified. Refresh by re-running /constitution. -->
# xstockstrat-indicators — Constitution (Python — formula engine (owns PLAT-03))

> Inherits all `PLAT-*` from the root constitution (`../../../docs/constitution.md`). Only
> module-specific invariants below.

> The behavioral contract (4 behaviors) is prepended to this target's `CLAUDE.md`, not stored here.

- **INDICATORS-01 (Floor).** Sandbox restricts builtins via a **private `__builtins__` exec namespace**, never by deleting from the shared module. `sandbox.py:144-153`; scar `b256d41`(#561).
- **INDICATORS-02 (Floor).** Memory cap uses `RLIMIT_DATA` (not `RLIMIT_AS`) + BLAS/OMP thread pinning. `sandbox.py:32-46`; scar `26cdf44`(#663).
- **INDICATORS-03.** System formulas (`author==SYSTEM_AUTHOR`) immutable for everyone — reject *before* the admin check. `:305-310`; scar `e6062f9`.
- **INDICATORS-04.** Formula authz is author-ownership primary, admin-0x04 override secondary — don't copy as the reference authz pattern. `:311-315`.
- Gotcha: seeded formula id UUIDv5 `d1ff5e6b-…c702b` — feature 062 depends on it. `fundamentals_value_quality.py:23`.

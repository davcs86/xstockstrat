"""Built-in formula definitions seeded at startup (feature 063)."""

# Reserved author marking platform-managed (built-in) formulas: UpdateFormula/DeleteFormula
# refuse to mutate them (even for admins) and the UI renders them read-only.
SYSTEM_AUTHOR = "system"

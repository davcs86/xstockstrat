<!-- Generated by constitution-forge (https://github.com/davcs86/agent-plugins) from static code + git-history analysis. AI-derived: every rule cites a path:line or commit, but the set has NOT been human-verified. Refresh by re-running /constitution. -->
# xstockstrat-agent — Constitution (Python MCP server (owns PLAT-05))

> Inherits all `PLAT-*` from the root constitution (`../../../docs/constitution.md`). Only
> module-specific invariants below.

> The behavioral contract (4 behaviors) is prepended to this target's `CLAUDE.md`, not stored here.

- **AGENT-01.** Admin write tools MUST call `_admin_metadata()` (injects `x-access-scope="7"`, not `"4"`); plain `_metadata()` → `PERMISSION_DENIED`. `app/client.py:32`.
- **AGENT-02.** `MCP_AGENT_SECRET` dual-purpose (header + OAuth `txn` HMAC) — rotate in an OAuth-quiet window. `client.py:26`, `oauth_server.py:41`.
- **AGENT-03.** `config` lookups strip the `agent.` namespace — pass bare keys. `client.py:635-639`.
- Gotcha: serve Streamable HTTP at root so Claude.ai detects tools; don't re-nest under `/sse`. Scar `e546fa5`.

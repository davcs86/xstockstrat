<!-- Generated by constitution-forge (https://github.com/davcs86/agent-plugins) from static code + git-history analysis. AI-derived: every rule cites a path:line or commit, but the set has NOT been human-verified. Refresh by re-running /constitution. -->
# xstockstrat-analysis — Constitution (Python — scoring & backtesting)

> Inherits all `PLAT-*` from the root constitution (`../../../docs/constitution.md`). Only
> module-specific invariants below.

> The behavioral contract (4 behaviors) is prepended to this target's `CLAUDE.md`, not stored here.

- **ANALYSIS-01.** Cell eligibility relies on implicit SQL-NULL semantics: `WHERE definition_fingerprint=$2` excludes NULL-fingerprint cells. Don't add an unfiltered fetch or a sentinel. `backtest_run_symbols.py:75`.
- **ANALYSIS-02.** Never raise out of a persist helper on the RunBacktest path (best-effort by invariant). `servicer.py`.
- **ANALYSIS-03.** Scoring scale anchors are hardcoded magic (`sharpe/2.0`, `1-drawdown/0.5`, buy floor `0.55`, sell `0.45`) while blend *weights* are config; don't simplify — backtest↔screener parity. `servicer.py:1547`; `scoring.py:39`.
- Note: the former "treat 0 as unset" rule is **not here** — reclassified to the root findings (Python zero-trap) per CF-N10.

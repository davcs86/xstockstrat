<!-- Generated by constitution-forge (https://github.com/davcs86/agent-plugins) from static code + git-history analysis. AI-derived: every rule cites a path:line or commit, but the set has NOT been human-verified. Refresh by re-running /constitution. -->
# xstockstrat-config — Constitution (Node — WatchConfig authority (owns the ConfigValue contract))

> Inherits all `PLAT-*` from the root constitution (`../../../docs/constitution.md`). Only
> module-specific invariants below.

> The behavioral contract (4 behaviors) is prepended to this target's `CLAUDE.md`, not stored here.

- **CONFIG-01 (Floor).** `ConfigValue` is a **`oneof`**; consumers MUST use `HasField`/`WhichOneof`, never truthiness — a present `0` is meaningful. (The Python zero-trap violates this → root findings.) `config.proto:48-55`.
- **CONFIG-02 (Floor).** Storage is snake_case; convert to camelCase before `.encode()` (ts-proto). `toProtoSnapPayload:21-60`; scar `1d2a03e`(#390).
- **CONFIG-03.** DB `key` column is the verbatim consumer lookup string, no namespace prepended; `secret.*` values are `secret://…` references, never plaintext. `migrations/007`.

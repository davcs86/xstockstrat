<!-- Generated by constitution-forge (https://github.com/davcs86/agent-plugins) from static code + git-history analysis. AI-derived: every rule cites a path:line or commit, but the set has NOT been human-verified. Refresh by re-running /constitution. -->
# xstockstrat-notify — Constitution (Node — streaming alerts)

> Inherits all `PLAT-*` from the root constitution (`../../../docs/constitution.md`). Only
> module-specific invariants below.

> The behavioral contract (4 behaviors) is prepended to this target's `CLAUDE.md`, not stored here.

- **NOTIFY-01 (Floor).** Live subscribers live in an in-process `Map`, **never on a DB connection** — `pool=1` is safe; do NOT bump `DB_POOL_MAX` for subscriber features (inverse of ledger #710). `notifyServiceImpl.ts:19,104-128`.
- **NOTIFY-02.** Fan-out is best-effort/lossy (failed `write()` silently evicts); don't add delivery guarantees. `:79-85`.
- **NOTIFY-03.** Severity crosses the DB boundary as int, elsewhere as the string enum; timestamps must be JS `Date`. Scars `389e25d`(#698),`0b64b99`(#443).

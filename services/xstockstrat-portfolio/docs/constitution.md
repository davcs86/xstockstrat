<!-- Generated by constitution-forge (https://github.com/davcs86/agent-plugins) from static code + git-history analysis. AI-derived: every rule cites a path:line or commit, but the set has NOT been human-verified. Refresh by re-running /constitution. -->
# xstockstrat-portfolio — Constitution (Go — positions & P&L)

> Inherits all `PLAT-*` from the root constitution (`../../../docs/constitution.md`). Only
> module-specific invariants below.

> The behavioral contract (4 behaviors) is prepended to this target's `CLAUDE.md`, not stored here.

- **PORTFOLIO-01 (Floor).** Broker mark-to-market is authoritative: any read path MUST go through `enrichPositions` (skip `current_price>0`), never its own quote loop. `portfolio_service.go:261,351`; scar `e6062f9`(#735).
- **PORTFOLIO-02.** `account.positions.synced`: `user_id` present → key under it, empty → `"default"`; valuation `0`=not reported → marketdata fallback. `:739-742`; scars `d8a6306`,`16da0b8`.
- **PORTFOLIO-03.** Realized P&L replayed at query time from ledger events where `source_service=="trading"`; wrong source → silent 0. `:411-506`.
- **PORTFOLIO-04.** `cost_basis` is TOTAL signed cost; mid=(ask+bid)/2 is the only fallback basis. `portfolio_repo.go:248`.

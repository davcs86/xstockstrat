# xstockstrat-portfolio — Constitution

Derived by `/context-constitution` (context-forge) on 2026-07-24; citations re-grounded 2026-08-27;
refreshed 2026-09-02 (branch `claude/loaded-plugins-list-d120nl` @ `82a0549` — feature 167 added
`UpdateWatchlistBinding` (PORTFOLIO-7 amended); added PORTFOLIO-11 offline-realized parity; anchors
re-grounded).
Captures the **non-obvious** local invariants of the portfolio service (position tracking, P&L, gRPC
50052). Does not restate documented/CI-enforced rules (see `## Pointers`).

> Inherits all rules of the root constitution (`../../../docs/context-constitution.md`). This file lists only
> what is specific to **xstockstrat-portfolio**.

## Rules (`PORTFOLIO-*`) — binding, easy-to-miss conventions

| ID | Rule | Why | Evidence | Example (canonical `path:line`) |
|---|---|---|---|---|
| **PORTFOLIO-1** | **Broker valuation is authoritative: `enrichPositions` skips any position the broker already valued (`CurrentPrice > 0`) and only falls back to a marketdata mid-quote for un-valued rows.** All read paths must enrich this way. | Recomputing `mid*Qty` for a broker-valued position overwrites the broker's mark, so the figure stops reconciling with broker equity — the exact bug migration 005 fixed. | `internal/service/portfolio_service.go:358` (`enrichPositions`, `CurrentPrice > 0` skip `:360`, comment `:334-337`); called `:494,525,547,1104` | `internal/service/portfolio_service.go:358` |
| **PORTFOLIO-2** | **`cost_basis` is stored as *total signed cost* (qty × avg), never per-share** — sells scale `CostBasis` proportionally while holding `AvgEntry`; sync writes `costBasis = qty * avgCost`. | The P&L math `MarketValue − CostBasis` and the winners/losers filter depend on it; storing per-share or recomputing at read time breaks unrealized P&L. | `portfolio_service.go:274` (`newCost = existing.CostBasis * (newQty / existing.Qty)`, block `:271-279`, `UpsertPosition:309`); `internal/repository/portfolio_repo.go:267` (`CostBasis`, cost_basis col `:59-62`) | `internal/repository/portfolio_repo.go:267` |
| **PORTFOLIO-3** | **All ledger consumers go through one reconnect/resume helper that resumes from `lastSeq+1` in memory and treats `codes.Unavailable` as a benign close.** | Logging every close at ERROR trips alerting on routine GOAWAYs; resuming from 0 double-counts incremental order fills. (Instance of root PLAT-N3.) | `portfolio_service.go:157` (`consumeEventStream`), `:180` (`streamEventsFrom`), resume `lastSeq+1` `:160-167,181-182`, benign-close `isGracefulStreamClose` `:210` | `internal/service/portfolio_service.go:157-182` |
| **PORTFOLIO-4** | **Ledger/balance sync payloads are parsed by hand-written JSON structs, not the proto type** — a producer field rename silently zeroes the field here. Match the emitter's JSON tags exactly. | `unrealized_pl`/`day_pnl` etc. and the `trading_mode` string form `"TRADING_MODE_LIVE"` are matched by tag/value; the producer is xstockstrat-trading. | `portfolio_service.go:218-230` (`orderFillPayload` struct + json tags, `StopPrice` at `:230` for feature 083's resting-stop learning), `TRADING_MODE_LIVE` match `:248`, applied `:388` | `internal/service/portfolio_service.go:218-230` |
| **PORTFOLIO-5** | **Resting-stop risk is learned by replaying trading's `order.filled` ledger events at boot — never a synchronous portfolio→trading call — and the risk fields compute on read off the broker-authoritative `CurrentPrice`.** | A direct portfolio→trading stop lookup would create a trading↔portfolio dependency cycle; stops therefore live in an in-memory `stopStore` (no portfolio migration) rebuilt from the ledger via the same full-history replay as positions. Extends PORTFOLIO-1's broker-valuation seam (feature 083). | `stopStore` `:56` (type `:71`, set `:80`, get `:90`); learned on fill `:253-254`; `enrichRisk:355` → `enrichPositionRisk:369` → `applyStopRisk:384`; risk fields `:388`; `portfolio_risk_test.go` | `internal/service/portfolio_service.go:369-388` |
| **PORTFOLIO-6** | **`GetPosition`'s error classification is a load-bearing cross-service contract**: `classifyGetPositionError` maps only `repository.ErrPositionNotFound` to `NotFound`; every other error (DB failure, timeout) returns `Internal`. | `xstockstrat-trading`'s REDUCE_ONLY kill-switch fails **closed** on *any* `GetPosition` error, not just `NotFound` (`internal/service/trading.go:3253-3276`, `GetPosition:3265`, `isExposureIncreasing:3230,3276`, feature 100). Changing this mapping back to a blanket `NotFound` would silently defeat trading's fail-closed halt behavior. Not documented in either service's CLAUDE.md. | `internal/handler/portfolio_handler.go:408` (`classifyGetPositionError`, `errors.Is:409`); `repository.ErrPositionNotFound` def `internal/repository/portfolio_repo.go:262`, raised in `scanPositionRow:283` | `internal/handler/portfolio_handler.go:408` |
| **PORTFOLIO-7** | **Watchlist writes carry `(symbol, strategy_id)` bindings with `bindings` taking precedence over the legacy flat `symbols` field, and `AddSymbols`/`AddWatchlistSymbols` can never rebind an existing symbol's `strategy_id`** (`ON CONFLICT (watchlist_id, symbol) DO NOTHING`). **A single-symbol rebind must use `UpdateWatchlistBinding` (feature 167)** — a surgical single-row `UPDATE ... SET strategy_id ... RETURNING` that preserves `source` provenance (structurally can't hit the fails-080 reset trap); `AddSymbols` still silently no-ops a rebind, and a full `Update` (delete+reinsert) resets provenance. | Fixes a "fails-080" trap where an `AddSymbols` call silently no-oped a strategy rebind attempt; feature 167 added the surgical path so a rebind needn't replace-all (which resets `source`). | `requestBindings` `internal/service/portfolio_service.go:1313`; `insertBindingsTx` `internal/repository/watchlist_repo.go:368` (`ON CONFLICT (watchlist_id, symbol) DO NOTHING` `:372`); surgical rebind `UpdateWatchlistBinding` `portfolio_service.go:1496` → `WatchlistRepo.UpdateBinding` `watchlist_repo.go:247` (single-row `UPDATE ... RETURNING symbol, strategy_id, source` `:258-262`) | `internal/repository/watchlist_repo.go:368` |
| **PORTFOLIO-8** | **`ListAllWatchlistSymbols` is the platform's FIRST cross-user enumeration of per-user data, and is gated by the `x-internal-caller` allow-list — NEVER the admin `x-access-scope` bit.** Every other watchlist/portfolio RPC is `x-user-id`-scoped self-service; this one deliberately returns *all* users' distinct watchlist symbols to one internal caller (grant `analysis-fundsignal`), read from incoming gRPC metadata, fail-closed. The admin bit is explicitly ignored (an admin-only caller is denied). | The admin bit authorizes a human's own scope + globals, **never another user's per-user rows** (PR #994); using it for a cross-user read would revive the feature-092-removed self-asserted-admin pattern. This is portfolio's first authz gate — there was none before — so the mechanism (direct incoming-metadata read, `{callerID, rpc}` least-privilege grant mirroring config's `authz.ts`) is a new convention future gated RPCs must follow. | `internal/service/authz.go` (`HeaderInternalCaller:11`, `internalCallerAllowlist:20` grant `:21`, `hasInternalCallerAuthority:33`); `ListAllWatchlistSymbols` `internal/service/portfolio_service.go`; repo `ListAllSymbols` `internal/repository/watchlist_repo.go:293` (`SELECT DISTINCT symbol` `:295`) | `internal/service/authz.go` |
| **PORTFOLIO-9** | **An offline account's `ConfirmOrder` emits `account.positions.synced` ONLY — never `order.filled`.** The two portfolio position consumers are disjoint by event type — `ConsumeOrderFills` (incremental fold) vs `ConsumePositionSyncs`/`GetPnL` (absolute reconcile) — so emitting both would double-count offline positions; `order.filled` is emitted only from the broker fill-poller, which skips offline accounts. | An editable offline confirmation re-runs a non-idempotent incremental fold with no broker snapshot to self-heal, so the offline producer recomputes absolute state and emits the reconciling snapshot instead (feature 157). Keeping the event types disjoint is what stops `GetPnL` Pass 1/2 and `ConsumeOrderFills` from folding offline fills twice. | trading `internal/service/trading.go:903` (offline-only gate), `:970-974` (sole `account.positions.synced` emit), `:1517` (`order.filled` broker-poller only), `:1435` (offline skip); portfolio consumers `internal/service/portfolio_service.go:144` (`ConsumeOrderFills`), `:901` (`processPositionSync`) | `services/xstockstrat-trading/internal/service/trading.go:970-974` |
| **PORTFOLIO-10** | **`account.positions.synced` is an *absolute* snapshot — its consumer runs `DeletePositionsNotInSync`, and an EMPTY positions set wipes the whole account.** Therefore any producer must emit **nothing** on a failed/uncertain recompute (return early without emitting), never an empty snapshot. (An empty set is used deliberately only on account deregistration.) | A failed offline recompute that emitted an empty snapshot is indistinguishable from a legitimate flat account and would delete every position; `ConfirmOrder` returns early without emitting on recompute failure, and the order edit is already persisted (feature 157). | consumer wipe `internal/repository/portfolio_repo.go:380` (empty `presentSymbols` → unconditional `DELETE` `:381-383`, comment `:379`), applied `internal/service/portfolio_service.go:978`; producer guard `services/xstockstrat-trading/internal/service/trading.go:929` (emit-nothing on failed recompute) | `internal/repository/portfolio_repo.go:380` |
| **PORTFOLIO-11** | **Offline realized-P&L (`GetOfflineRealized`, proto3 presence) must populate on EVERY portfolio read path** so an offline `$0` (`proto.Float64(0)`) stays distinguishable from a broker `unset` (parity invariant, C-10(b)). | Adding/editing a portfolio read path without the `GetOfflineRealized` lookup makes offline accounts show `RealizedPnl=nil` there — reopening the `ListPositions`↔`ListPortfolios` parity gap features 157/159 closed. Related: the sync-payload `realized_pnl` is a `*float64` whose *presence* is the offline/broker discriminator, and its upsert lands OUTSIDE the positions loop (`:986`) so a full-close recompute (`positions: []`) still records account-grain realized. | `GetOfflineRealized` calls `internal/service/portfolio_service.go:512-514` (`GetPortfolio`), `:1132-1134` (`buildAccountPortfolio`); sync-payload `RealizedPnl *float64` `:886`, applied `:986` | `internal/service/portfolio_service.go:512,1132` |

## Gotchas & scars

- **Three compute paths never got migrated to broker-authoritative valuation** (PORTFOLIO-1) — `GetPnL`, `broadcastSnapshot` equity, and `checkRiskLimits` still recompute from mid-quotes, so for broker-synced positions their numbers diverge from what `ListPositions`/`GetPortfolio` show. Recorded as a latent bug in findings; noted here so an agent doesn't copy the deviant loops. Evidence: `portfolio_service.go` `GetPnL:~530`, `broadcastSnapshot:~701`, `checkRiskLimits:~736` (none call `enrichPositions`).
- **SEV-1 scar (root PLAT-8): `WatchConfig` silently subscribed at zero-value scope until 2026-08-07.** `config.Watcher` declared `environment`/`tradingMode` fields but `NewWatcher` never populated them (identical bug fixed identically in marketdata/trading in the same commit, `1413399`). Fixed via `resolveEnvironment`/`resolveTradingMode` (`internal/config/config.go:64-97`, wired `cmd/server/main.go:46`). (Feature 147 later dropped the `trading_mode` request half.)

## Candidate rules (unverified)

| Candidate | Why suspected | What would confirm it |
|---|---|---|
| `buildAccountPortfolio` passes empty `trading_mode`, mixing PAPER+LIVE rows for one account | `portfolio_service.go:1094` (`buildAccountPortfolio`, `ListPositionsByAccount(ctx, accountID, "")` `:1095`) single site | confirmation that an account_id never spans modes |

## Pointers (already documented or CI-enforced — not restated here)

| What | Where |
|---|---|
| pgxpool cap=2 / `DB_POOL_MAX`; PgBouncer exec-mode (root PLAT-7) | `internal/repository/pool.go:16-41` (`defaultMaxConns:16`, `QueryExecModeExec:37`); root pool budget |
| Header propagation interceptor; `x-user-id` read from header for ownership | `internal/middleware/propagation.go:27-35` (root PLAT-4/PLAT-5) |
| gRPC keepalive on inter-service dials | `portfolio_service.go:101` (`clientKeepAlive`, dials `:113-121`) (root PLAT-N3) |

---
_Forged by [context-forge](https://github.com/davcs86/agent-plugins). It captures the
non-obvious — nothing here is invented; re-run `/context-constitution` to refresh after the code changes._

<!-- Generated by constitution-forge (https://github.com/davcs86/agent-plugins) from static code + git-history analysis. AI-derived: every rule cites a path:line or commit, but the set has NOT been human-verified. Refresh by re-running /constitution. -->
# xstockstrat-marketdata — Constitution (Go — Alpaca feed & OHLCV)

> Inherits all `PLAT-*` from the root constitution (`../../../docs/constitution.md`). Only
> module-specific invariants below.

> The behavioral contract (4 behaviors) is prepended to this target's `CLAUDE.md`, not stored here.

- **MARKETDATA-01.** `GetLatestQuote` returns raw ask/bid, **no mid** — consumers compute it & guard `0` (iex closed-market). `marketdata_repo.go:231`; scar `067c2aa`(#656).
- **MARKETDATA-02.** `GetFundamentalsMulti` is the ONLY fundamentals path; order-preserving, **drops** missing symbols. `:880-885`.
- **MARKETDATA-03.** Canonical timeframes `15m/1h/1d`; translate to Alpaca only for the fetch URL, store canonical. Scar `4d8d440`(#695).
- **MARKETDATA-04.** FMP client built once at boot; live toggle needs a restart. `main.go:111`.

linters:
  enable:
    - gofmt
    - govet
    - errcheck
    - staticcheck
    - gosimple
    - ineffassign
    - unused
    - goimports

linters-settings:
  goimports:
    local-prefixes: github.com/xstockstrat

run:
  timeout: 5m

<!-- Generated by constitution-forge (https://github.com/davcs86/agent-plugins) from static code + git-history analysis. AI-derived: every rule cites a path:line or commit, but the set has NOT been human-verified. Refresh by re-running /constitution. -->
# xstockstrat-ingest — Constitution (Python — normalization & signals)

> Inherits all `PLAT-*` from the root constitution (`../../../docs/constitution.md`). Only
> module-specific invariants below.

> The behavioral contract (4 behaviors) is prepended to this target's `CLAUDE.md`, not stored here.

- **INGEST-01.** `IngestSignal` does **NOT** dedup (bare INSERT, no unique constraint) — idempotency is the caller's job. `servicer.py:658-677`.
- **INGEST-02.** `source_type` allow-list must stay a superset of the DB CHECK; adding a type is dual-touch. `signal_sources.py:75-118`; scar #768.
- **INGEST-03.** `IngestSignal` rejects unregistered/inactive sources; `direction ∈ {buy,sell,hold,watchlist}`. `:639-648`.
- Gotcha: Struct **input** decode uses `MessageToDict`, **output** uses `dict()`-style — reverting input reintroduces #768. `:873` vs `:836`.

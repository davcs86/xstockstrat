<!-- Generated by constitution-forge (https://github.com/davcs86/agent-plugins) from static code + git-history analysis. AI-derived: every rule cites a path:line or commit, but the set has NOT been human-verified. Refresh by re-running /constitution. -->
# xstockstrat-trading — Constitution (Go — order execution)

> Inherits all `PLAT-*` from the root constitution (`../../../docs/constitution.md`). Only
> module-specific invariants below.

> The behavioral contract (4 behaviors) is prepended to this target's `CLAUDE.md`, not stored here.

- **TRADING-01 (Floor).** Async ledger/notify emits use `context.Background()` → no propagated headers; `user_id` MUST be hand-copied into the payload. `trading.go:315…`; scar `d8a6306`(#648).
- **TRADING-02.** Poller shape: ticker + re-read `*.interval_ms` each cycle + `ticker.Reset`; `<=0` disables. `trading.go:629,766,1113`.
- **TRADING-03.** `config.Watcher.Get*` returns proto zero when a key exists (default only when absent) → guard `<=0`. `config.go:152-180`.
- **TRADING-04.** Snapshot the broker pool under `RLock` into a local map, iterate lock-free; never hold the lock across a broker REST call. `trading.go:654,821,1136`.
- Gotcha: Config Watcher race fix (`sync.Once`+channel-close+RWMutex+backoff) is load-bearing. Scar `5d174a7`(#391).

<!-- Generated by constitution-forge (https://github.com/davcs86/agent-plugins) from static code + git-history analysis. AI-derived: every rule cites a path:line or commit, but the set has NOT been human-verified. Refresh by re-running /constitution. -->
# xstockstrat-identity — Constitution (Node — auth, JWT, OAuth)

> Inherits all `PLAT-*` from the root constitution (`../../../docs/constitution.md`). Only
> module-specific invariants below.

> The behavioral contract (4 behaviors) is prepended to this target's `CLAUDE.md`, not stored here.

- **IDENTITY-01.** Signs `roles` as a string array (`jsonwebtoken`, not `jose`); the scope bitmap is derived in the UI (refines PLAT-04). `identityServiceImpl.ts:75`.
- **IDENTITY-02.** JWT secret is env-only (`process.env.JWT_SECRET`, throws if unset), NOT config; byte-identical across identity + all frontends. `:28-36`; scar `98b1c29`(#417).
- **IDENTITY-03.** Refresh tokens/auth codes stored SHA-256-hashed; bcrypt for passwords only; JWT claims snake_case internal. `:84,342`.

<!-- Generated by constitution-forge (https://github.com/davcs86/agent-plugins) from static code + git-history analysis. AI-derived: every rule cites a path:line or commit, but the set has NOT been human-verified. Refresh by re-running /constitution. -->
# xstockstrat-ui — Constitution (Next.js — consolidated BFF)

> Inherits all `PLAT-*` from the root constitution (`../../../docs/constitution.md`). Only
> module-specific invariants below.

> The behavioral contract (4 behaviors) is prepended to this target's `CLAUDE.md`, not stored here.

- **UI-01 (Floor).** A new page MUST be registered in `PLATFORM_SUBNAV` (`PlatformHeader.tsx:79`) or it's unreachable. Scar `e6062f9`; `fails.md:41`.
- **UI-02 (Floor).** Header literals + admin bit `0x04` are ESLint-banned except in `headers.ts`/`auth.ts` — import, never inline. `.eslintrc.json:6-24`.
- **UI-03.** A service under two segments needs two browser clients keyed by which BFF gates the method. `browserClients/insightsMarketDataClient.ts:5-9`.
- **UI-04.** Server-side identity injection overrides client values, but the field name varies per RPC (`userId`/`author`/header-only). `insightsBff.ts:88-97`.

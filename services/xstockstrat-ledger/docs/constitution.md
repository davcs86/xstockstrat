<!-- Generated by constitution-forge (https://github.com/davcs86/agent-plugins) from static code + git-history analysis. AI-derived: every rule cites a path:line or commit, but the set has NOT been human-verified. Refresh by re-running /constitution. -->
# xstockstrat-ledger — Constitution (Node — append-only event store)

> Inherits all `PLAT-*` from the root constitution (`../../../docs/constitution.md`). Only
> module-specific invariants below.

> The behavioral contract (4 behaviors) is prepended to this target's `CLAUDE.md`, not stored here.

- **LEDGER-01 (Floor).** Pool is a hard **1 query + 1 LISTEN/NOTIFY** split; idempotent lookup reuses the txn's own client after ROLLBACK — never a 2nd `pool.connect()` (deadlock at max=1). `ledgerServiceImpl.ts:81-93`; scar `4222dca`(#710).
- **LEDGER-02 (Floor).** `StreamEvents` subscribes **before** replay, buffers, flushes deduped-by-sequence + flips `live=true` synchronously. Never replay-then-subscribe; never hold a pooled conn for the stream. `:211-273`.
- **LEDGER-03.** `sequence` is a global monotonic column DEFAULT, never set by code; `event_id` server `uuidv4()`. scar `def20aa`(#639).
- **LEDGER-04.** NOTIFY payload snake_case, **truncated to 5 fields >7000 bytes**; route through `rowToEvent` (camelCase). `migrations/001:69-82`; scar `4124ae1`(#605).
